# CLAUDE.md — نظام إدارة الاستثمار في نينوى (Nineveh Investment Command System)

> هذا الملف هو **القانون الأعلى** للمشروع. يُقرأ في بداية كل جلسة ويُلتزَم حرفياً. عند أي تعارض، CLAUDE.md والوصف الشامل يفوزان على أي اقتراح. **مرجع الحقيقة النهائي هو النظام المبنيّ فعلياً؛ والوصف الشامل (v2.0) يصفه ويُزامَن معه — فإن خالف الوصفُ المبنيَّ، يُصحَّح الوصف ليطابق المبنيّ.**

## بروتوكول كل جلسة (إلزامي — قبل أي عمل)
1. **اقرأ `docs/وصف_النظام_الشامل.md` بالكامل** + `docs/خريطة_البناء.md` + `docs/سجل_البناء.md` (**سجلّ المنجَز والحالة = فهرس الذاكرة المعتمد**؛ لا يوجد `MEMORY.md` منفصل)، وأكّد فهمك باختصار قبل البدء.
2. قبل بناء أي جزء: **أعد قراءة قسمه المعني من الوصف** ولا تكتب سطراً قبل ذلك.
3. نفّذ **حرفياً** وفق الوصف — لا زيادة ولا نقصان ولا تأويل.
4. **لا تفترض أبداً:** عند أي غموض أو نقص، ارجع للوصف؛ وإن لم تجد إجابة، **توقّف واسأل المستخدم** — لا تخمّن ولا تكمّل من عندك.
5. عند أي تعارض: الوصف + هذا الملف يفوزان على أي اقتراح أو ذاكرة.

## المصدر الأوحد للحقيقة
- **المواصفات الكاملة:** `@docs/وصف_النظام_الشامل.md` (v2.0 — يصف النظام المبنيّ فعلياً) — **اقرأها بالكامل قبل أي بناء**. كل بناء جديد يطابقها حرفياً، وتُزامَن مع المبنيّ عند كل مرحلة (عند التعارض: **المبنيّ يفوز ويُصحَّح الوصف**).
- **خريطة البناء بالمراحل:** `@docs/خريطة_البناء.md` — ابنِ مرحلةً مرحلة وفقها.
- **بيانات حقيقية:** مجلد `/data/` — لا تُؤلَّف ولا تُعدَّل يدوياً (استيراد بسكربت فقط).

## ما نبنيه
تطبيق ويب **بمستخدمَين بدورَين** (مدير كامل الصلاحية + مستخدم عرض مقيَّد ينشئه المدير) لهيئة استثمار نينوى، محوره **خريطة تفاعلية** تربط القطع بالفرص والرخص والشركات والقانون. ثنائي الغاية: **قرار سيادي + ترويج للمستثمر**. الجودة المطلوبة: نظيف · رصين · ثابت · مبهر · **صفر خطأ**.

## الحزمة التقنية (ثابتة)
- **Frontend/full-stack:** Next.js (App Router) + TypeScript **strict** + Tailwind + shadcn/ui + Framer Motion — RTL
- **الخريطة:** MapLibre GL JS + MapTiler (متّجه) + deck.gl (طبقات WebGL)
- **القاعدة:** Supabase (PostgreSQL + PostGIS + pgvector/HNSW + Auth + Storage + Realtime)
- **الذكاء:** Anthropic API عبر **الخادم فقط** (Server Actions / API Routes) — RAG على pgvector
- **الحالة:** TanStack Query · **التصدير:** PDF خادمي (Puppeteer/react-pdf)
- **النشر:** Render (التطبيق) + Supabase (القاعدة)

## قواعد غير قابلة للتفاوض (صفر استثناء)
1. العربية + **RTL** في كل الواجهة. **الأرقام لاتينية دائماً (0123).**
2. **صفر تأليف:** كل رقم/ادّعاء من مصدر. الناقص يُعرَض **«غير متوفّر»** لا يُخترَع.
3. **الاستشهاد:** اسم القانون + سنة + مادة + بند. **يُمنع** عرض المعرّف الداخلي للسجل.
4. **لا كشف للتحقّق الداخلي** للمستخدم إطلاقاً.
5. **المنطق القانوني (الوصف §ج.9) = دوالّ نقية حتمية مع اختبارات وحدة — ليس ذكاءً.** الذكاء يصوغ الشرح فقط، لا يقرّر نتيجة قانونية.
6. **مفاتيح API لا تصل للعميل أبداً** — كل نداء ذكاء خادمي.
7. تمييز **🟦 إلزامي** (باستشهاد) عن **🟩 اجتهاد** (مؤشَّر «غير مُلزِم»).
8. **تحقّق مسبق من الرخص** قبل أي إجابة عن قطعة/فرصة.
9. النموذج المستخدم في **كل** وظائف الذكاء = **المختار في إعدادات المستخدم**.

## التصميم (الوصف §هـ.3)
- السمة الأساسية **داكنة = كحلي مات** + توهّج هولوكرامي؛ العناصر بيضاء/تدرّجات كحلية.
- حالات القطعة الخمس بألوانها: معلَنة `#C7A24E` · قيد الإنجاز `#5775A8` · منجزة `#5E977A` · مسحوبة `#B5616A` · مفترضة `#22C3F3`.
- الخطّ **Readex Pro** (Google Fonts) · تنبيهات toasts سلسة · القطاعات نصّ لا لون.

## الأعراف الهندسية
- بنية مجلدات **حسب الميزة** (feature-based).
- **أنواع TypeScript موحّدة** من نموذج البيانات (§ج.8) كمصدر واحد للأنواع.
- **طبقة عرض مركزية** تفرض قواعد المخرجات (§ح): استشهاد · أرقام لاتينية · منع المعرّف الداخلي · منع كشف التحقّق.
- **اختبارات وحدة** لمحرّك الضوابط (§ج.9) + طبقة المخرجات + منطق المطابقة.
- ESLint + Prettier · التزامات Git صغيرة وموصوفة · فرع لكل مرحلة.

## أسلوب العمل (التزِم به)
- **خطّة أولاً** (Plan Mode) لكل مرحلة قبل الكتابة. **مرحلة واحدة في كل مرة** (راجع `@docs/خريطة_البناء.md`).
- بعد كل ميزة: راجِع مقابل قسم الوصف المعني + اكتب اختبارات + التزام Git.
- لا تنحرف عن الوصف؛ عند الالتباس **ارجع إليه لا تفترض**.

## ممنوعات
- تأليف بيانات/قوانين/أرقام.
- كشف معرّفات داخلية أو حالة تحقّق.
- تعديل ملفات `/data/` يدوياً.
- أرقام عربية-هندية في الواجهة.
- تعليقات مفرطة · تخزين أسرار في الشيفرة.
