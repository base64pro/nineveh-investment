# سجل البناء — نظام إدارة الاستثمار في نينوى

> **ما هو هذا الملف:** سجلّ **تاريخ التنفيذ** لما أُنجِز فعلياً (م0 → م8.12.1 · **النظام منشور حيّ على Render وقيد الاستخدام الفعلي**). يُقرأ في بداية كل جلسة **بعد** البروتوكول والوصف وخريطة البناء، ليستأنف أي جلسة جديدة من حيث انتهت سابقتها **بنفس الجودة**.
> **ليس** مصدراً للمواصفات. المرجع الأوحد للحقيقة هو **النظام المبنيّ فعلياً**، و`docs/وصف_النظام_الشامل.md` يصفه كما هو الآن (مُزامَن في الإصدار 2.0). عند أي تعارض: **النظام المبنيّ يفوز** وتُصحَّح الوثائق لتطابقه.

---

## بروتوكول الجلسة الجديدة (إلزامي — نفّذه بالترتيب قبل أي كود)
1. اقرأ `CLAUDE.md` كاملاً (القانون الأعلى).
2. اقرأ `docs/وصف_النظام_الشامل.md` **كاملاً** + `docs/خريطة_البناء.md`.
3. اقرأ **هذا السجل** (هو **فهرس الذاكرة المعتمد** — لا يوجد `MEMORY.md` منفصل).
4. أعد قراءة **قسم الوصف الخاص بالمرحلة التالية** بدقّة (م3 = §هـ.4 نافذة القطعة والإجراءات + §ج.9 محرّك الضوابط).
5. قدّم **خطّة تفصيلية** للمرحلة، **انتظر موافقة المستخدم**، ثم أنشئ فرع المرحلة وابنِ — مرحلةً واحدة في كل مرة، توقّفاً للمراجعة بعد كل خطوة كبيرة.
6. القواعد غير القابلة للتفاوض (CLAUDE.md): عربية RTL · **أرقام لاتينية دائماً** · صفر تأليف («غير متوفّر» لا يُخترَع) · استشهاد بالقانون لا بالمعرّف الداخلي · **لا كشف للتحقّق الداخلي** · المنطق القانوني §ج.9 دوالّ نقية حتمية مع اختبارات (ليس ذكاءً) · المفاتيح خادمية فقط.

---

## الحالة العامة للمراحل
| المرحلة | الوصف | الحالة | الفرع/الوسم |
|---|---|---|---|
| م0 | الأساس (سقالة · مخطّط · أنواع · استيراد · مصادقة) | ✓ مكتملة | `main` (عبر `m0-foundation`) |
| م1 | أساس الخريطة (MapLibre + MapTiler + الحدود) | ✓ معتمدة | `main` + وسم `m1-approved` |
| م2 | الطبقات + أقسام السايدبار الخمسة | ✓ **معتمدة** | `main` + وسم **`m2-approved`** |
| م3 | نافذة القطعة والإجراءات (§هـ.4 + §ج.9) | ✓ **معتمدة** | `main` + وسم **`m3-approved`** |
| م4 | القانون والذكاء (RAG · pgvector/HNSW) | ✓ **معتمدة** | `main` + وسم **`m4-approved`** |
| م5 | الهيدبار · البحث · التقارير · الإعدادات | ✓ **معتمدة** | `main` + وسم **`m5-approved`** |
| م6 | تصدير PDF · طبقة المخرجات §ح · المتانة §ز · تابات الذكاء | ✓ **منفّذة** (بانتظار الاعتماد) | فرع `m6-export-polish` |
| م7 | حزم التحسين المعتمدة (رسم متقدّم · صور · بحث محرَّر · تقارير مبهرة · هيدبار/سايدبار/جارت · شاشة دخول) | ✓ **منفّذة** (بانتظار الاعتماد) | فرع **`m7-enhancements`** (يتفرّع من م6) |
| م6.5 | النشر على Render + فحص دخاني | ⏳ بعد اعتماد التجربة المحلية | — |

> **النشر مؤجَّل حتى ما بعد م6** (سياسة الاختبار المحلّي بعد كل مرحلة). إعداد Render جاهز: Build `npm install && npm run build` · Start `npm run start` · Node 22 · المتغيّرات الستّة.

---

## الحزمة التقنية (ثابتة)
- **الواجهة:** Next.js 15 (App Router) · TypeScript **strict** (`noUncheckedIndexedAccess`) · Tailwind v3.4 · shadcn/ui · Framer Motion v11 · RTL · خط **Readex Pro**.
- **الخريطة:** MapLibre GL JS + MapTiler (متّجه عبر وسيط خادمي) + **deck.gl** (`@deck.gl/core`،`/layers`،`/mapbox`،`/extensions`) + **terra-draw** (+ محوّل maplibre) + **@turf** (centroid · booleanPointInPolygon · bbox).
- **القاعدة:** Supabase (PostgreSQL + PostGIS + RLS «المصادَقون فقط») عبر `@supabase/ssr`؛ الهجرات بـ`scripts/migrate.ts` (تتبّع `schema_migrations`، `npm run migrate`)؛ تحقّق بـ`pg`.
- **الحالة/المزامنة:** TanStack Query (قراءات) + Server Actions (كتابات) + Supabase Realtime (إبطال).
- **الذكاء (م4):** Anthropic عبر الخادم فقط · RAG على pgvector.

---

## م0 — الأساس ✓
- **السقالة:** Next 15 + TS strict + Tailwind + shadcn + RTL + Readex Pro + next-themes + TanStack Query + Vitest.
- **المخطّط (§ج.8/§ج.5):** 11 جدولاً عبر هجرات `supabase/migrations/` — snake_case إنجليزي · إحالات رخوة · FK واحدة (`legal→legal_documents`) · RLS · PostGIS.
- **الأنواع:** `src/types/entities.ts` (§ج.8) + خريطة تسميات الشركات.
- **الاستيراد (سكربت فقط، لا تأليف):** مُطبَّق ومؤكَّد في القاعدة: **27 فرصة · 146 رخصة · 491 شركة · 8 وثائق/125 سجلاً قانونياً**.
- **المصادقة:** `@supabase/ssr` (جلسات بالكوكيز) + `src/middleware.ts` يحمي كل المسارات (يستثني `/api`) + `/login` عربية + أفعال login/signOut. anon محجوب (RLS).
- **أوامر:** `migrate:dry` · `migrate` · `import` · `verify:db` · `verify:rls`. **DATABASE_URL = Session pooler 5432.**

## م1 — أساس الخريطة ✓ (وسم `m1-approved`)
- **MapLibre مباشر** + إضافة RTL مستضافة في `public/vendor` + تسميات `name:ar`.
- **قاعدة كحلية داكنة** (`streets-v2-dark`) + مبدّل (داكن/فاتح/قمر=hybrid).
- **وسيط `/api/maptiler`** يحقن المفتاح خادمياً ويزيله من JSON (يبني العنوان بـ`new URL` حفظاً للرمز `@`؛ النمط `no-store`).
- **حدود `/api/boundaries/{governorate|districts|subdistricts}`** من `data/map` (1/9/28).
- `buildStyle` يحقن الحدود + قناع التعتيم في كائن النمط (تظهر من أوّل إطار وعند تبديل القاعدة) · `maxBounds` = اتحاد الصندوق∪المنظر · معالج `styleimagemissing` (يمنع المربّعات السوداء).
- **تنبيه:** لا تشغّل `prettier --write .` على الجذر — `.prettierignore` يستثني `CLAUDE.md`/`README`/`docs`.

---

## م2 — الطبقات وأقسام السايدبار ✓ (وسم `m2-approved`)

### م2.1 — البنية المشتركة
- **طبقة البيانات** (`src/lib/data/`): `use-table.ts` (قراءة TanStack، مفتاح `["table",name]`) · `actions` لكل ميزة (Server Actions للكتابة) · `realtime.ts` (إبطال؛ `PARCEL_TABLES` تُبطِل `["map_parcels"]`) · `use-counts.ts` · `use-field-options.ts` + `field-options-actions.ts`.
- **طبقة العرض المركزية §ح** (`src/lib/display.ts`): أرقام لاتينية · «غير متوفّر» للناقص · **منع المعرّف الداخلي** · منع كشف التحقّق.
- **القشرة** (`src/features/shell/`): `app-sidebar.tsx` (8 أيقونات أقسام) + `sections.ts` + خطّ فاصل متوهّج + خلفية أفتح.
- **التصدير:** `src/lib/export-csv.ts`. **الفلتر الموحّد:** `src/components/ui/filter-combo.tsx` (منسدلة أنيقة، خيار «الكل»، فلترة بالكتابة، تنقّل لوحة المفاتيح).

### م2.2 — أقسام السايدبار الخمسة (CRUD كامل)
كل قسم بالقالب نفسه (راجع ذاكرة `m2-section-template`): `*-panel` (قائمة + فلاتر FilterCombo + تصدير + **بطاقات قابلة للطي**) · `*-form` · `*-detail` · `actions.ts` · `fields.ts`.
- **الفرص** `src/features/opportunities/` — 27 سجلاً · حالة `announced` فقط.
- **الرخص** `src/features/licenses/` — 146 سجلاً · **4 تصفيات حالة** (الكل/قيد/منجزة/مسحوبة) بألوان الحالات + `StateBadge` ملوّن + **سجلّ زيارات** `visits/` (يرتبط بـ`parcel_ref = String(record_id)`، للرخص قيد/منجزة فقط). أُضيف الحي + الناحية المدعومة بمنسدلة.
- **الشركات** `src/features/companies/` — 491 سجلاً · **لا حالة** ⇒ فلتر + **شارات الأهلية** (مؤهّلة = غير مستثناة وتستوفي عتبة §ج.9) · المحافظة ISO→عربي · `id` نصّي = **معرّف داخلي لا يُعرَض** (§ح) · `id` جديد = `crypto.randomUUID()`.
- **مكتبة المعايير** `src/features/criteria/` — **محرّر بنود ديناميكي** + تفعيل/تعطيل.
- **تصميم فرصة (المفترضة)** `src/features/assumed/` — CRUD حالة `assumed` + حقل **اسم** إلزامي + رسم/ربط بالخريطة. PK uuid (insert بلا id).
- **المعاجم:** `src/lib/sectors.ts` (16 قطاعاً EN→AR) · `src/lib/governorates.ts` (18 ISO→AR) · `src/lib/nineveh-geo.ts` (9 أقضية + 28 ناحية **حرفياً من بيانات الخريطة، لا تأليف**).
- **حالات القطعة الخمس وألوانها** (`src/features/parcels/state-badge.tsx` عبر أصناف `tailwind.config.ts`): معلَنة `#C7A24E` · قيد الإنجاز `#5775A8` · منجزة `#5E977A` · مسحوبة `#B5616A` · مفترضة `#22C3F3` (أزرق رقميّ، حدّ متقطّع — صُحِّح من #8B6FB0 لمطابقة المبنيّ).

### م2.3 — التلاشي الهرمي للحدود
- في `overlayLayers()` بـ`investment-map.tsx`: **تلاشٍ هرمي بالزوم** (محافظة←أقضية←نواحٍ) تفاعلي.
- **المقاييس:** أرقام (المعلَنة/الرخص/المفترضة) أسفل تسميات الأقضية/النواحي عبر `applyStats`+`augmentStats`. المنسوب حالياً = القطع المرسومة (لعدم وجود ناحية في بيانات الفرص/الرخص ⇒ يلزم استنتاج مكاني، أُنجِز في م2.4).

### م2.4 — تلوين/رسم/استنتاج/تنقّل القطع
بنية الخريطة المتقدّمة (المكوّن المركزي `src/features/map/components/investment-map.tsx` ~600 سطر):
- **المصدر:** view `map_parcels` (هجرة `..120003` ثم `..120007`) — يوحّد المفترضة (من `assumed_parcels.geom`) والفرص/الرخص (من `parcel_geometry` بالمطابقة على `parcel_no`+`muqataa_no`)، ويُخرِج: `kind · ref_id · parcel_no · state · geometry (GeoJSON عبر st_asgeojson) · label`. **تنبيه:** أعمدة PostGIS تُقرأ EWKB عبر REST ⇒ الـview ضروري لإخراج GeoJSON.
- **القراءة:** `src/features/map/lib/use-map-parcels.ts` (مفتاح `["map_parcels"]`) → `FeatureCollection`. الألوان: `parcel-colors.ts`.
- **العرض (deck.gl `MapboxOverlay`، interleaved:false، فوق م1):**
  - `GeoJsonLayer` — ملء شفّاف + حدّ + **هالة هولوكرامية**؛ المحدّدة تمتلئ بلون/شدّة الحدّ (ألفا≈232) وتخفت ما حولها.
  - `IconLayer` (`parcel-markers.ts`، canvas، حجم ثابت) — **إشارة دبوس لكل قطعة:** ساق أبيض + حلقة بيضاء تحاوط القرص + لبّ بلون الحالة. **كل الإشارات ظاهرة دائماً** (أُزيل `CollisionFilterExtension` بقرار المستخدم — لا إخفاء/تذبذب).
- **الرسم والربط:** `terra-draw` (+ محوّل maplibre) — زرّ عائم، مضلّع بنفسجي؛ عند الإنهاء:
  - استنتاج مكاني `src/features/map/lib/spatial-inference.ts` (`inferName`: turf centroid ∩ booleanPointInPolygon → `name_ar`).
  - فرع: قطعة جديدة → RPC `create_assumed_parcel(p_geom,p_district,p_subdistrict)` + فتح نموذج المفترضة؛ أو ربط بسجل قائم → RPC `create_parcel_geometry(p_geom,p_parcel_no,p_muqataa_no)`. كلاهما `ST_GeomFromGeoJSON→ST_Multi`، security invoker.
- **التنقّل (pub/sub):** `src/features/map/lib/map-nav-store.ts` — `requestFlyTo/onFlyTo` · `requestOpenParcelForm/onOpenParcelForm` · `requestOpenParcelDetail/onOpenParcelDetail` · `requestStartDraw/onStartDraw`. النوافذ العامّة `assumed-modals.tsx` في القشرة تستمع للأحداث.
- **نقر القطعة:** deck `pickObject` → تحديد/تظليل (`autoHighlight`)؛ نقر الفراغ يلغي. **نقر الإشارة:** نافذة عمودية أنيقة بجانب القرص — **عنوان القطعة** (`label`) ثم زرّ «عرض» (→`requestOpenParcelDetail`) ثم زرّ «الموقع» (→`requestFlyTo`).
- **في البطاقات:** زرّ **«الموقع المباشر»** (`src/components/ui/live-location-button.tsx`، أبيض دائري، `stopPropagation`) بجانب الاسم (المفترضة) أو القطاع (الفرص/الرخص) → `requestFlyTo`؛ وزرّ **«ارسم»** (عند وجود رقم قطعة) → `requestStartDraw`.
- **لوحة تبديل الطبقات** (الحدود/القطع) أزرار عائمة يمين الخريطة + زرّ «كامل نينوى».

### خريطة ملفات م2 (مرجع سريع)
```
src/lib/data/            use-table · use-counts · use-field-options · field-options-actions · realtime
src/lib/                 display(§ح) · export-csv · sectors · governorates · nineveh-geo · supabase/{client,server}
src/components/ui/       button · dialog · filter-combo · live-location-button · option-field · skeleton
src/features/shell/      app-sidebar · sections
src/features/parcels/    state-badge
src/features/opportunities/  panel · form · detail · actions · fields
src/features/licenses/   panel · form · detail · actions · fields · status-counters · visits/{log,actions}
src/features/companies/  panel · form · detail · actions · fields
src/features/criteria/   panel · form · detail · actions · fields
src/features/assumed/    panel · form · detail · modals · actions · fields
src/features/map/components/  investment-map (المحرّك)
src/features/map/lib/    use-map-parcels · parcel-colors · parcel-markers · spatial-inference · map-nav-store
supabase/migrations/     ..130001_realtime → ..120007_map_parcels_label (هجرات م2)
```

### القرارات المثبّتة (م2)
- أعمدة snake_case إنجليزي · الأنواع من `src/types/entities.ts` مصدراً واحداً.
- القطاعات/المحافظات تُعرَض بالعربية عبر المعاجم (البيانات تخزّن رموزاً).
- **هندسة القطع يرسمها المستخدم بنفسه بعد البناء (§ج.10)** — النظام يبني الأدوات جاهزة للاستقبال؛ `map_parcels` فارغة حتى الرسم.
- الاختبارات: 10/10 (محرّك أوّلي + منطق). `next lint` مُهمَل → يُرحَّل لـESLint CLI في م6.

---

## المؤجَّل بالمراحل — التزام صريح (لا إهمال؛ كلٌّ يُحقَّق في مرحلته)
- **م3:** ✓ **مُنجَزة ومعتمدة** (نافذة القطعة الموحّدة + التابات الأربعة + محرّك §ج.9 + النقل عبر الأنواع + الربط بشركة + الزيارات + توحيد عرض/تعديل + منسدلة Combo موحّدة). **بقي لـم4:** التوليد الذكي للتوصيات/المعايير (RAG) — التابان 2/3 شِلّ.
- **م4:** المستشار القانوني الذكي (RAG) + **تضمينات vector + فهرسة HNSW** + التوصيات الذكية + **الإثراء بالويب للشركات** + بطاقات الذكاء.
- **م5:** الهيدبار (عدّادات KPI + البحث الذكي الموحّد) · التقارير (لوحات/تشارتات) · إعدادات المستخدم (الحساب/العرض/النموذج والمفاتيح/التصدير) · مبدّل السمة/الكثافة · صور الزيارة (Storage).
- **م6:** طبقة المخرجات §ح مفروضة كلياً · **تصدير PDF براندد** · التلميع البصري §هـ.3 · ترحيل ESLint CLI · الطبقة المحرَّرة `map_elements` · **النشر على Render**.

---

## م3 — نافذة القطعة والإجراءات ✓ (وسم `m3-approved`)
- **نافذة القطعة الموحّدة** (`src/features/parcels/parcel-window.tsx`) للأنواع الثلاثة: رأس ثابت + تحرير مباشر + لا كشف تحقّق. **قالب واحد**: «عرض» (سايدبار/خريطة) = قراءة فقط (fieldset disabled)، «تعديل» = تحرير؛ أُزيلت قوالب `*-detail.tsx` الثلاثة؛ النموذج لـ«إضافة» فقط. + حقل الشركة + سجلّ الزيارات + `transfer_log`.
- **النافذة الفرعية للإجراءات** (`actions-window`) + 4 تابات: (1) **محرّك الضوابط الحتمي §ج.9** (`legal/controls-engine.ts`: 9 ضوابط + 6 معايير · باستشهاد · تكييف بالحالات · خلاصة أهلية) + **23 اختباراً** + واجهة `controls-tab`. (2)/(3) التوصيات/المعايير = شِلّ ← م4. (4) `report-tab` (عرض داخلي؛ PDF=م6).
- **النقل عبر الأنواع** (RPC ذرّي `move_parcel`): نقل كامل + هندسة + زيارات + حذف المصدر + **`transfer_log` (صفر فقدان)** + **استعادة تلقائية** عند النقل لنوع أغنى + رقم مؤقّت يحفظ رسم المفترضة. منسدلة الحالات الخمس في النافذة.
- **منسدلة موحّدة `Combo`** (+ `ComboField`) بتصميم `FilterCombo` حلّت محلّ كل `<select>`/`datalist`. + بطاقة إشارة الخريطة ملمَّعة + زرّ سجلّ الزيارات في بطاقات الرخص.
- **القبول ✓:** تاب الضوابط يطابق §ج.9 باستشهاد ويجتاز اختباراً لكل بند؛ التكييف بالحالات صحيح. الاختبارات **33/33**.

## م4 — القانون والذكاء (RAG) ✓ (وسم `m4-approved`)
- **بنية الذكاء (خادمية):** `src/lib/ai/anthropic.ts` (Messages) + `voyage.ts` (تضمينات) — المفاتيح لا تصل العميل. ⚠️ المفتاح **`APP_ANTHROPIC_KEY`** (بيئة Claude Code تحجز ANTHROPIC_API_KEY وتحقنه فارغاً) · `scripts/env.ts` بـ`override:true`.
- **التضمينات (RAG):** عمود `legal.embedding vector(1024)` + فهرس HNSW · RPC `match_legal` (استرجاع دلالي + استشهاد) · سكربت `embed:legal` (Voyage، **بالبنود**، 122 سجلاً). طبقة Voyage المجانية: 3 RPM/10K TPM ⇒ تباطؤ.
- **المستشار القانوني** (§هـ.5 · قسم `legal-advisor`): شقّان — **أسئلة حرّة** (RAG → Opus 4.8 باستشهاد · ٦ حالات إجابة · عرض منسّق `AdvisorAnswer` برقائق استشهاد) + **استمارة استعلام** (تعيد محرّك §ج.9 `ControlsTab`) — لوحة مبوّبة + **مكتبة الاستشارات** (حفظ بعنوان ذكي/فتح/حذف).
- **م4.4:** صياغة الذكاء لشرح الضوابط (`legal/explain-actions` + زرّ «اشرح بالذكاء» في `ControlsTab`) — **شرح لا قرار**.
- **القبول ✓ (مُختبَر فعلياً):** المستشار يجيب باستشهاد دقيق (قانون+سنة+مادة+بند) · يميّز الاجتهاد · **صفر تأليف** (خارج النطاق «غير متوفّر»).
- **مؤجَّل (ويب، باختيار المستخدم):** توصيل تابَي التوصيات/المعايير + إثراء الشركات (يحتاج مزوّد بحث ويب).

## م5 — الهيدبار والبحث والتقارير والإعدادات ✓ (وسم `m5-approved`)
- **م5.1 الهيدبار (داشبورد §هـ.1):** RPC `dashboard_stats` حتمية + خطاف + إبطال Realtime · `shell-store` (ناقل فتح الأقسام) · شريط علوي بعدّادات ملوّنة بالحالات (نقر ← المصدر) + هوية الهيئة وصورة المدير (يسار، `public/director.jpeg` بإطار هولوكرامي) · **متجاوب يلتفّ بلا تمرير أفقي** (تخطيط الصفحة عمودي مرن).
- **م5.2 البحث الفائق (§هـ.2.ج · Ctrl K):** RPC `super_search` حتمي (فرص/رخص/شركات/مفترضة) + **تطبيع عربي** `ar_normalize` (ا/أ/ة/ه/ى/تشكيل) + `map_ref`/`has_geom` (المرسوم يطير · غيره يفتح سجلّه) · `maptiler-geocode` خادمي **دائماً بالتوازي** (تسميات الخريطة الأصلية: أحياء/مناطق/معالم ضمن نينوى حصراً) · الذكاء يفهم ويوجّه (بنية فقط، لا تأليف) · لوحة palette.
- **م5.3 التقارير الذكية (§هـ.5 · سايدبار · حتمي):** `report-aggregations` دوالّ نقية + **٦ اختبارات** · لوحة KPIs + رسوم **Recharts** بثيمة النظام (حالات/قطاع/قضاء/زمن) + فلاتر لحظية + النقر للمصدر + تصدير CSV (PDF=م6).
- **م5.4 الإعدادات (§هـ.5):** هجرة `settings` + `app_secrets` (**RLS يمنع قراءة العميل** — مُختبَر: `permission denied`) · عميل `service` خادمي + `ai-config` (النموذج/المفاتيح من القاعدة ثم env) · ربط `anthropic`/`voyage` · لوحة 4 تابات (حساب/عرض/ذكاء+مفاتيح مقنّعة ••••/تصدير) · **النموذج المختار يحكم كل وظائف الذكاء (القاعدة #9)**.
- **جولة صقل (تُحسَب م6 جزئياً):** مربّعات إظهار القطع حسب الحالة · تجربة ما بعد النقل (رسالة وجهة + طيران) · إصلاح حركة عدّادات الرخص · تنبيهات أنيقة (sonner مخصّص) · أيقونة إغلاق احترافية · **زر مسح التصفية** + ترقية قسم الفرص لتصميم الرخص (ORB مشترك) · إصلاح نموذج الزيارة المتداخل + تحصين الرسم.
- **القبول ✓:** العدّادات من بيانات حقيقية · البحث يجد أبسط عنصر وتسميات الخريطة · الإعدادات تضبط النموذج/المفاتيح فعلياً (مؤكَّد). typecheck/lint نظيفان · **39 اختباراً ناجحاً**.
- **مؤجَّل لـم6:** تصدير PDF · تابات الذكاء (توصيات/معايير/إثراء — تحتاج مزوّد بحث ويب).

## م6 — التصدير والمتانة وتابات الذكاء ✓ (فرع `m6-export-polish` — بانتظار الاعتماد)
- **تصدير PDF خادمي** (Puppeteer متصفّح مفرد + خط Readex محلي base64): تقرير القطعة بـ5 نطاقات (قطعة/ضوابط/توصيات/معايير/شامل) + تقارير الجداول (غلاف + رسم SVG) + تقرير الاستشارة — مسارات `/api/pdf/*` محمية بالجلسة · هوية من الإعدادات · استشهادات · لا معرّف داخلي.
- **§ز المتانة:** شريط انقطاع الاتصال · مهلة وكيل MapTiler 15ث + إعادة محاولة → 502 · فشل الذكاء برسالة + إعادة محاولة (لا تلفيق) · الضوابط الحتمية تعمل دون ذكاء.
- **§ح مفروضة:** تدقيق شامل (أرقام لاتينية · «غير متوفّر» · لا معرّفات · لا كشف تحقّق).
- **تابات الذكاء:** التوصيات + إنشاء المعايير (RAG + بحث ويب `web_search` بقرار الإعدادات، 🟩 مؤشَّرة، تثبيت/مسح في `parcel_insights`) + إثراء الشركات (قائمة حقول مسموحة + مصدر إلزامي).
- مراجعة بناء كاملة + تنفيذ كل إصلاحاتها (منها: إصلاح انقلاب `is_excluded` عند حفظ الشركة).

## م7 — حزم التحسين المعتمدة ✓ (فرع `m7-enhancements` — بانتظار الاعتماد)
- **الرسم المتقدّم:** وضعيات مستطيل/مربّع/دائرة/بأبعاد + تحرير حدود المرسوم (`update_parcel_geom`) + استوديو رسم قابل للطي.
- **التسميات المحرَّرة** (`map_elements` + `create_map_element`): تسمية معلم/منطقة على الخريطة، تظهر بالبحث الفائق (rank أعلى) وعلى الخريطة بطبقاتها.
- **صور المشروع** (Storage خاص `parcel-photos` بروابط موقّتة، حتى 12 صورة) + صور الزيارات (3) — تتبع النقل عبر `move_parcel` v4.
- **شارة القطعة المتتبعة:** بطاقة هولوكرامية بخط قائد يتبع القطعة مع الزوم/التنقّل (لا تظليل).
- **التقارير المبهرة** (داخلياً + PDF) + **نسيج الزمكان** المائي داخل نينوى + خريطة أغمق وأحدّ + هيدبار حيّ متجاوب + سايدبار بأيقونات مسمّاة ومؤشّر أحادي + جارت هولوكرامي منسّق مع الأدوات العائمة + **شاشة دخول هولوكرامية**.
- فلترة الظهور حسب الحي + الحالات + الحدود + التسميات (لوحة طبقات بمنسدلة حرّة).

## الحالة التشغيلية المحلية (جلسة 2026-06-12)
- **بناء إنتاجي ناجح** (`npm run build`: 9 صفحات · صفر أخطاء) و**خادم إنتاجي محلي** يعمل مفصولاً: `next start` على :3000 (سجلّه `.next/start.log`) — جاهز للتجربة المحلية بسرعة كاملة.
- typecheck + lint نظيفان · **45 اختبار وحدة** ناجحة · §ح مدقَّقة (لا أرقام هندية بالواجهة · لا تسريب معرّفات).
- **إدارة الخادم:** إيقاف = `Stop-Process` على PID المنفذ 3000 · تطوير = أوقفه ثم `npm run dev` · **لا `next build` أثناء عمل dev** (ذاكرة `dev-build-next-cache`).

## م7.7 — إصلاحات تدفّق معتمدة (جلسة 2026-06-12)
- **إصلاح انقلاب الحالة عند الحفظ (نافذة القطعة):** الجذر = `LICENSE_FORM_FIELDS` يتضمن `status` والنافذة لا تعرضه (يُدار بمنسدلة النقل) فكانت حلقة الحفظ ترسله `null` و`saveLicense` يفرض «قيد الإنجاز» حتى عند التحديث. العلاج المزدوج: (أ) حارس `fd.has(key)` في `parcel-window` — لا يُرسَل حقل غير معروض (يحمي الفئة كلها)؛ (ب) مطبّع نقي `normalizeLicenseStatusForSave` (افتراضي عند الإنشاء فقط؛ التحديث بلا حالة لا يمسّها) + **5 اختبارات وحدة**. تدقيق الفئة: `saveOpportunity` سليم · `saveAssumed` يثبّت `assumed` (طبيعي — النقل عبر `move_parcel`).
- **توحيد قواميس المنسدلات (هجرة `..120028`):** view `field_value_options` = المعرّف يدوياً (`field_options`) ∪ القيم المستعملة فعلاً في الفرص/الرخص/المفترضة (حي/قضاء/ناحية/مقاطعة/عائدية/نوع حق/جنسية/تخطيط/وضع قانوني/نوع مشروع/قطاع — بمطابقة أعمدة §ج.8 حرفياً). `useFieldOptions` يقرأ الـview ← كل النماذج ونوافذ القطع اكتسبت الاتحاد تلقائياً + دمجها في فلاتر اللوحات (فرص/رخص/مفترضة/تقارير) وفلتر حي الخريطة. Realtime: تعديل أي جدول قطع يبطل القاموس ← القيمة الجديدة تظهر فوراً في كل المنسدلات. مُتحقَّق حياً: 10 أحياء (تشمل المعرَّفة من النوافذ) · 113 مقاطعة · 52 عائدية.

## م7.8 — إصلاحات جذرية متحقَّقة E2E (جلسة 2026-06-13، تحقيق متعدد الوكلاء)
- **فشل حفظ الرخص (مستنسَخ حياً 23502):** `licenses.status` NOT NULL بلا default + upsert بلا الحقل ← Postgres يفحص NOT NULL على صف الإدراج قبل تحكيم التعارض. العلاج: **فصل insert/update** في saveLicense وsaveOpportunity (التحديث يضبط المرسَل فقط؛ الإنشاء يفشل بصوت عند تصادم معرّف). متحقَّق E2E: حفظ رخصة منجزة ← نجاح + الحالة باقية + سيل الإبطالات.
- **تعطل الرسم بعد تبديل القاعدة (مثبَت من dist المكتبات):** d.stop() داخل idle كان يرمي بعد مسح setStyle لمصادر td-* فلا يصل start (try/catch مشترك) ← \\_enabled=false دائمة وأزرار ميتة بصمت. العلاج: **stop قبل setStyle** + فصل الحمايات + **شفاء ذاتي** `if (!d.enabled) d.start()` في كل مداخل الرسم. متحقَّق E2E: مضلّع نشط بعد فاتح←قمر←داكن (3 تبديلات متتالية).
- **نافذة إشارة القطعة ← MarkerCallout هولوكرامية** (بخط ربط يتبع الإشارة حيّاً، z فوق طبقات deck فلا تختفي خلف الإشارات) بدل Popup؛ حارس «عرض» لرسمة بلا كيان (entity_id فارغ ← تنبيه بدل صمت).
- **حذف الرسمة (فك الارتباط §هـ.4):** RPC `delete_parcel_geom` (هجرة 120029) + زر «حذف الرسمة» في شارة القطعة — فرصة/رخصة: حذف صف الهندسة؛ مفترضة: geom=null والسجل يبقى. متحقَّق على صف اصطناعي.
- **شارة القطعة 340px:** الصورة ~80% (300px) بقراءات زجاجية فوقها + **Lightbox** (`photo-lightbox.tsx`: تكبير ×6 عجلة/أزرار، سحب، تنقل، مصغّرات).
- **هجرة 120030 (تدقيق):** move_parcel v5 يرحّل description/raw_details/project_type/source_url (كانت تُحبس في transfer_log) + قيد هندسة `unique nulls not distinct` (منع تكرار القطعة عند muqataa_no فارغ) + upsert هندسة النقل.
- **§ز معمَّم:** res.error يظهر في كل toasts الفشل (نافذة/نماذج/تسميات/رسم) · فشل حفظ الرسم لا يُتلف الشكل · switchBase محصّن (فشل الجلب يرجع للقاعدة السابقة بتنبيه) · useTable مرتّب بالمفتاح (ترقيم مستقر) · تحذير سقوط بحث البيانات في النافذة الفائقة.
- العنوان: «مكتب السيد رئيس الهيئة الأستاذ حارث البخو». ضجيج كونسول AdobeClean = إضافة Acrobat بالمتصفح (خارجي).
- **مؤجَّل موثَّق:** conditions jsonb كنص حر · اشتراك settings بالتزامن · ترقيم التسميات >1000 · undo للحذف · opportunities بلا subdistrict (فجوة نموذج).

## م7.9 — حزمة الإنتاجية والتجربة (جلسة 2026-06-13، متحقَّقة E2E)
- **تصدير PDF بالبطاقة:** زر PDF في بطاقات الفرص/الرخص/المفترضة (تقرير القطعة scope=parcel — مُختبَر: 200/PDF/133KB) وبطاقات الشركات (قالب الجدول البراندد لصف واحد). مساعد `export-parcel-pdf.ts`.
- **تقارير الزيارات** (`reports/visits-report.tsx` + تبويبا لوحة التقارير): سجلّ مُثرى ببيانات رخصته، فلاتر مركّبة (من/إلى تاريخ · قضاء · موظف منفّذ · قطاع · حالة قطعة · نوع زيارة) + تصدير PDF — متحقَّق بصرياً.
- **مربع سؤال المستشار يتوسّع مع النص** حتى 50% من لوحة الاستشارات ثم تمرير (متحقَّق: 55←158px مع overflow ثم انكماش).
- **فلتر الأحياء أعيد جذرياً:** قائمة مدمجة مرئية دائماً (بحث + تمرير scroll-slim + شارة المختار) بدل المنسدلة المنبثقة — استئصال فئة «لا تظهر إلا برفريش» كاملةً (متحقَّق: 10 أحياء تظهر فور أول فتح). + انبثاق متدرّج لوضعيات استوديو الرسم.
- **البحث الفائق:** طبقة فورية من ذاكرة الخريطة للقطع المرسومة (أولوية مطلقة، مطابقة احتوائية بأي جزء/وسط كلمة بتطبيع عربي محلي) + دمج نتائج الخادم خلفها بلا تكرار + debounce ‏200ms (متحقَّق: «ربيعي» تُظهر «جزيرة أم الربيعين» أولاً).
- **أصوات الواجهة** (`lib/sfx.ts` + `sfx-events.tsx`): توليف WebAudio خالص — نقرة رقمية لكل زر/تاب (مستمع مفوَّض واحد) + ومضة انبثاق للبطاقات (شارة/إشارة/نافذة القطعة) + انسياب طيران عند الانتقال للقطعة. مكتومة رصينة، بلا ملفات.
- **الأحدث أولاً افتراضياً** في قوائم الفرص/الرخص (record_id تنازلياً) والمفترضة (created_at تنازلياً).

## م8.1 — نظام مستخدمَين بأدوار (RBAC · جلسة 2026-06-13، متحقَّق E2E)
> **تحديث §ب.5:** صار النظام **مستخدمَين بأدوار** بدل مستخدم واحد (المدير يصنع الثاني من الإعدادات).
- **الأمان على القاعدة (الفرض الحقيقي):** هجرة `..120031` — جدول `app_users(user_id, username, role admin|viewer)` + دالّة `is_admin()` (security definer) + بذر أقدم مستخدم = admin + إعادة كتابة RLS كاملة (قراءة للجميع؛ الكتابة السيادية للمدير؛ المعايير/التوصيات/الاستشارات/قيم القوائم: المصادَق يُدرِج والمدير يعدّل/يحذف؛ رفع/حذف صور التخزين للمدير). هجرة `..120032` — حُرّاس `is_admin()` صريحة على RPCs الرسم/النقل (رسائل خطأ نظيفة). **متحقَّق E2E بمستخدم viewer حقيقي:** SELECT ينجح · UPDATE/DELETE/INSERT سيادي يُرفض (RLS 42501/0-صفوف) · INSERT criteria/parcel_insights ينجح · move_parcel مرفوض.
- **الواجهة:** `features/auth/role-context` (RoleProvider + useRole) يُغذّى من `page.tsx`؛ بوّابات `isViewer` تخفي: قسمَي تصميم فرصة + الإعدادات · استوديو الرسم وأزرار الحدود/حذف الرسمة · أزرار إضافة/تعديل/حذف في كل اللوحات (تبقى عرض + تصدير + PDF) · منسدلة نقل الحالة + حفظ نافذة القطعة (للقراءة) + إضافة الزيارات/الصور. يبقى للـviewer: تابات الإجراءات (توصيات/معايير/تقرير) + «معيار جديد» + المستشار.
- **إدارة المستخدم الثاني:** تبويب «المستخدمون» في الإعدادات (للمدير) — `upsertSecondUser`/`getSecondUser` بدور service. **دخول باسم المستخدم:** `usernameToEmail` (اسم بسيط ← `name@nineveh.local`؛ بريد كامل كما هو) + اختبار وحدة.
- **متحقَّق بصرياً:** المدير يرى 8 أقسام + استوديو رسم؛ الـviewer 6 أقسام بلا رسم، الإضافة/التعديل/الحذف مخفية والعرض/PDF ظاهران، «معيار جديد» باقٍ. **المستخدم أنشأ مستخدمه الثاني الحقيقي «manager» فعلياً أثناء الجلسة** (الميزة تعمل end-to-end). الاختبارات 54/54.

## م8.2 — إعادة تصميم عرض الجوال (iPhone 17 Pro Max · عمودي · جلسة 2026-06-14، متحقَّق حيّاً عند 440×956)
> **النطاق:** الجوال فقط (`< md` / `<768px`) — **صفر تغيير على md+/الديسكتوب** (متحقَّق عند 800/1280px: الريل والهيدبار والعدّادات سليمة، الدوك/البحث/KPI الجوالة مخفية). صفر مساس بالمنطق/البيانات/الأدوار/RLS. الحدّ موحَّد `matchMedia("(max-width: 767px)")`.
- **الأسس (§2):** متغيّرات safe-area (`--sat/--sab/--sal/--sar`) + `touch-action: manipulation` للعناصر التفاعلية + حقول الإدخال ≥16px (جوال) + إلغاء وميض اللمس. **PWA إرشاد ناعم (قرار المالك — لا حجب):** `manifest.webmanifest` (standalone/portrait/أيقونات، بلا Service Worker التزاماً بـ§ب.4) + `appleWebApp` (`black-translucent`) + بانر تثبيت اختياري قابل للتجاهل (iOS Safari فقط).
- **القشرة (§3/§4/§7):** هيدبار الجوال = صورة أكبر + عنوان أوضح بلا زرّ بحث + إزاحة `--sat`. **دوك عائم** على الحافة اليمنى الفعلية (`right-2`، RTL) بنفس `SECTIONS` المفلترة بالأدوار + خروج، يعلو الورقة عند فتحها. شريط KPI أفقي رفيع تحت الهيدبار (يُعيد استخدام `CHIPS/NUM_GRADIENT`). توجيه الأقسام: الفرص/الرخص ← **ورقة سفلية**؛ البقية ← **ملء شاشة** (نفس مكوّنات اللوحات بلا تغيير منطقي).
- **الخريطة (§5):** عرض كامل على الجوال (`right-0 md:right-20`). تأطير المحافظة وكل طيران عبر **camera padding** (لا تغيير حجم canvas) يحترم شريط KPI والورقة الحيّة وشريط البحث؛ أزرار زوم MapLibre زجاجية مكبّرة مرفوعة فوق شريط البحث. الحدود من `bbox(nineveh_governorate.geojson)` القائم (لا تأليف).
- **الورقة السفلية (§6):** التقام «نصف/موسّع» بسحب `framer-motion` (transform فقط). **اللحظة البصرية:** نقر زر الموقع في البطاقة ← طيران سينمائي + الورقة إلى «نصف» + تطفو بطاقة صور القطعة (جوال فقط) + القائمة مفتوحة. مصدر واحد لارتفاع الورقة الحيّ (`mobile-sheet-store`) يستهلكه camera padding + قصّ البطاقة + إزاحة الدوك. شريط البحث السفلي (§8) يُخفى تحت الورقة.
- **بطاقة الصور (§9):** أبعاد مرنة على الجوال (عرض يتّسع للشاشة + ارتفاع محجوز ضمن الباند المرئي فوق الورقة، صورة `flex-1`)؛ الديسكتوب يبقى 340×446 حرفياً؛ الخط من **مركز القطعة** (centroid قائم).
- **ملفات جديدة:** `manifest.ts` · `components/{install-hint,mobile-search-bar}.tsx` · `shell/{mobile-dock,mobile-section-sheet,mobile-sheet-store}.ts(x)`. **معدّلة:** `globals.css · layout · page · headbar · mobile-kpis · app-sidebar · investment-map · selected-parcel-card`.
- **مراجعة تخاصمية متعددة الوكلاء:** أُصلحت ملاحظة md+ حقيقية (حارس بطاقة القطعة كان بعرض الـcanvas → صار `matchMedia` viewport، يمنع تحوّل البطاقة لوضع الجوال في النطاق 768–847px) + تنظيفات (RTL safe-area للدوك، توجيه مكرّر، كتابة CSS var ميتة). الاختبارات 54/54 · typecheck/lint نظيفان · كونسول نظيف.

## م8.3 — تحسينات عرض الجوال (حزمة طلب · جلسة 2026-06-14، متحقَّق حيّاً عند 440×956)
> نفس قيد م8.2 (جوال فقط، صفر تغيير على md+ — متحقَّق عند 1280px والبحث الديسكتوب سليم). تعديلات بصرية/تفاعلية على حزمة م8.2.
- **KPI (§7):** أرقام أكبر وأبهى (16px extrabold بتدرّج هولوكرامي) في أقراص زجاجية بارزة بنقاط متوهّجة.
- **الدوك (§4):** أيقونات أكبر (size-12/25px) + **مؤشّر نشط منزلق** (`layoutId` بزنبرك framer-motion + توهّج هولوكرامي) — موشن جرافيك متقدّم عند الاختيار.
- **الورقة السفلية (§6):** أرضية زجاجية ساحرة (وهج هولوكرامي + حدود متوهّجة)، رأس مكثّف (مساحة أكبر للبطاقات)، **انبثاق تلقائي إلى 50%** بحركة نابضة، و**تمرير منصبّ (coverflow):** البطاقة في المنتصف تكبر (scale ~1) والمجاورة تصغر/تخفت تدريجياً — يُدار من الورقة بلا مساس بمنطق اللوحات (مراقبة `.overflow-y-auto` + rAF + MutationObserver).
- **سلوك زر الموقع (معدّل بطلب):** نقر الموقع ← طيران + **تُغلَق الورقة تلقائياً**، و**لا تنبثق بطاقة الصور** (حصراً عند النقر على رسمة القطعة على الخريطة). الخريطة في الـ50% العلوية (الورقة تفتح 50%).
- **الدبابيس:** تنحيف (W 46→42، orbR 12.5→11، ساق أرشق) + هالة هولوكرامية أبهى. **تسميات ~1كم:** بطاقات زجاجية رشيقة باسم القطعة المختصر فوق الدبابيس المرئية تظهر عند الزوم (≥~13.5) وتتلاشى خارجه (تتبّع حيّ بـrAF، cap 26، تحترم إخفاء الحالات/القطع — لا تأليف).
- **أزرار الزوم (§5):** زجاجية ثلاثية الأبعاد أكبر (58px)، و**نُقلت للحافة اليسرى السفلى** (RTL: flex column + `align-items:flex-end`) لإزالة تداخلها مع الدوك اليميني؛ المقياس فوقها.
- **المستشار (الاستشارات):** `MobileFullscreen` يستخدم `height: var(--app-h)` فيثبت الإطار فوق الكيبورد عند تحرير النص.
- **البحث المضمَّن (§8):** شريط البحث السفلي صار **حقل البحث نفسه** (لا مربّع منفصل ينبثق)؛ عند التركيز يتمدّد لملء المنطقة المرئية وتظهر النتائج فوقه. المنطق مستخرَج إلى `useSuperSearch` يُعيد استخدامه الديسكتوب (SearchOverlay) والجوال (MobileSearch).
- **ملفات جديدة:** `components/mobile-search.tsx` · `features/search/use-super-search.ts`. **محذوف:** `mobile-search-bar.tsx`. **معدّلة:** `globals.css · page · mobile-kpis · mobile-dock · mobile-section-sheet · investment-map · parcel-markers · search-overlay`.

## م8.4 — صقل الجوال (حزمة طلب · جلسة 2026-06-15، متحقَّق حيّاً عند 440×956 + 1280px)
> نفس القيد (جوال فقط، صفر تغيير على md+).
- **زر «كامل نينوى»:** زر دائري بنصّه تحت الدوك (الوضع العمودي) يعيد العرض لكامل المحافظة عبر قناة `requestResetView`/`onResetView` الجديدة في `map-nav-store` (الاشتراك في `investment-map`). زر الريست في عنقود الأدوات صار `max-md:hidden` (يبقى للديسكتوب).
- **الدوك يتجانس مع الورقة:** عند فتح الفرص/الرخص يتحوّل الدوك إلى **شريط أفقي يعلو الورقة ويرتفع معها حيّاً** (bottom=ارتفاع الورقة) بدل الانزياح الرأسي الشاذّ.
- **الورقة 65%:** الانبثاق التلقائي = 65% من الشاشة (منطقة تمرير أوسع والخريطة أظهر)، والموسّع ≈90%.
- **KPI متحرّك (marquee):** الأرقام تمشي بحركة بطيئة سلسة لا نهائية (تدخل من اليمين) — **CSS خالص** (`@keyframes kpi-marquee`)، لا framer.
- **إصلاح حرج متحقَّق:** كان `motion.div` بحركة `repeat: Infinity` (نسخة framer من الـmarquee) **يوقف `AnimatePresence`** فلا تُغلَق أي لوحة قسم (الورقة وملء الشاشة على الجوال **ولوحة الديسكتوب الجانبية** أيضاً، لأن framer يحرّك حتى عنصراً `display:none`). التحويل إلى CSS أصلح الإغلاق على كل المسارات. متحقَّق: إغلاق الورقة/ملء الشاشة/الـaside يعمل، والريست يعيد لكامل نينوى، والديسكتوب سليم.
- **coverflow لكل البطاقات + تأكيد الطيران (حزمة طلب لاحقة):** البؤرة صارت **أعلى-وسط المنطقة المرئية** (40%) لا منتصف الحاوية (الحاوية أطول من المرئي بسبب transform)، مع حشوة علوية صغيرة تُبرز البطاقة الأولى وحشوة سفلية كبيرة (خارج الشاشة) تتيح الأخيرة بلوغ البؤرة، وإعادة حساب بعد استقرار حركة الفتح (380/780ms) — فكلّ البطاقات (الأولى والأخيرة ضمناً) تتمركز وتبرز عند تمريرها. **متحقَّق حيّاً E2E:** نقر زر الموقع على قطعة مرسومة (محطة تعبئة وقود السهل النموذجية) ← الورقة تُغلَق فوراً + الخريطة تطير للقطعة (50كم→100م) + تظهر تسميتها، بلا بطاقة صور تلقائية، بلا أخطاء كونسول. (القطع غير المرسومة: تُغلَق الورقة + تنبيه «لا حدود مرسومة» — لا طيران بلا إحداثيات، لا تأليف.) 54/54 اختبار · typecheck/lint/بناء نظيف · الديسكتوب سليم.

## م8.6 — صقل عرض الجوال (حزمة طلب · 2026-06-20 · مدموج في main ومدفوع لتحديث النشر · متحقَّق حيّاً عند 440×956 + ديسكتوب 1280px)
> نفس القيد (جوال فقط `< 768px` · صفر تغيير على md+ — متحقَّق · صفر مساس بالمنطق/البيانات/الأدوار/RLS). بناء إنتاجي ناجح + **54/54 اختبار** + typecheck/lint نظيف، ثم fast-forward إلى main ودفع `origin/main`.
- **زر «كامل نينوى»** (`mobile-dock.tsx`): فُصِل من صندوق الدوك إلى **قرص ثلاثي الأبعاد مستقلّ تحته** (حاوية عمودية)، أيقونة فقط بلا نص، تدرّج شعاعي + ظلال خارجية/داخلية + غوصة عند الضغط. صون قناة `requestResetView` وشرط `!horizontal` و`md:hidden`؛ زر الديسكتوب منفصل لم يُمَسّ.
- **شريط KPI** (`mobile-kpis.tsx` + `globals.css`): استبدال marquee المتحرّك بـ**صفّين ثابتين (3+3)** (`grid-cols-3`) — كل الأقراص ظاهرة دفعةً واحدة بلا تمرير أفقي؛ حُذفت `@keyframes/.kpi-marquee` الميتة. مطابقة الارتفاع الجديد (70px): `framePadding` KPI 46→74 (`investment-map.tsx`) و`topPad` بطاقة الصور 48→74 (`selected-parcel-card.tsx`) — كلاهما جوال فقط.
- **coverflow** (`mobile-section-sheet.tsx` · `useFocusedScroll`): **scroll-snap proximity + center align** (التقام مغناطيسي)، إزالة الحشوة العلوية المقحَمة (كانت ~150px فوق p-3 ← لا فراغ فوق أول بطاقة)، **تمريرتان قراءة-ثم-كتابة + تطبيق مباشر بلا transition يطارد القيمة** + `will-change` (إزالة الاهتزاز)، إبراز بؤرة المنتصف (scale 0.12/fade 0.32)، احترام `prefers-reduced-motion`.
- **تحرير المستشار** (`advisor-chat.tsx`): على الجوال (بحارس `matchMedia`) **Enter = سطر جديد** (الإرسال بالزر فقط) + **طيّ الكيبورد بعد الإرسال** + `enterKeyHint=enter`. الديسكتوب بلا تغيير (Enter=إرسال · Shift+Enter=سطر — متحقَّق حيّاً عند 1280px). الإطار مثبَّت فوق الكيبورد (`var(--app-h)`) قائم.

## م8.7 — تحسينات عامة (جوال + شاشات كبيرة · حزمة طلب · 2026-06-20 · مدموجة في main ومدفوعة لتحديث النشر)
> بناء إنتاجي ناجح + **54/54 اختبار** + typecheck/lint نظيف. صفر مساس بالمنطق/البيانات/الأدوار/RLS.
- **صوت النقر** (`sfx-events.tsx`): كان يُطلَق على pointerdown فيُسمَع عند بدء تمرير البطاقات (جسمها `role=button`). صار ببوّابة pointerdown→pointerup بعتبة حركة 10px ← صوت عند النقر الفعلي فقط، صمت عند أي تمرير/سحب (جوال + ديسكتوب). `unlockSfx` يبقى على pointerdown (سياسة autoplay).
- **تسميات القطع** (`investment-map.tsx`): عتبة الظهور `SHOW` 13.5→12 (تبدأ عند ~5كم بدل ~1كم)، وإزالة قصرها على الجوال (حذف حارس matchMedia و `md:hidden`) ← تظهر على **كل الشاشات**، ورفع `cap` 26→60 مع فرز بالأقرب لمركز الشاشة (أولوية حتمية لا اقتطاع عشوائي).
- **شريط KPI الجوال** (`mobile-kpis.tsx`): من صفّين (`grid-cols-3` · م8.6) إلى **صفّ واحد من ستة أقراص متطابقة** (`grid-cols-6`) بلا تمرير أفقي. KPI 74→50 و topPad 74→50 (ارتفاع الصف الواحد 48px).
- **تمرير الفرص/الرخص (coverflow)** (`mobile-section-sheet.tsx`): إعادة تصميم `useFocusedScroll` — **إبراز عند الاستقرار فقط**: صفر كتابة transform أثناء السحب (تسطيح البطاقات)، وعند التوقّف (`scrollend` + احتياطي debounce) تُبرَز بؤرة المنتصف بانتقال 0.24s. إزالة `content-visibility` عن بطاقات الفرص/الرخص (كان يفسد القياس ويسبّب الاهتزاز). متحقَّق: تباين المقياس أثناء السحب 0.00 (ثابت تماماً).
- **السايدبار العائم على الشاشات الكبيرة (≥1024px · §هـ.1)** (`page.tsx`/`app-sidebar.tsx`/`status-counters.tsx`/`globals.css`/`investment-map.tsx`): على lg تصبح الخريطة ملء الشاشة، والشريط دوكاً عائماً (`inset-y-3`/`right-3`/`rounded-2xl`/ظلّ) بأيقونات + نص مختصر، واللوحات تطفو يساره؛ مع إزاحة أدوات الخريطة العائمة وزوم MapLibre وعدّادات الرخص يسار الدوك. التابلت (md 768-1023) يبقى على الشريط الثابت؛ الجوال بلا تأثير. متحقَّق حيّاً (lg يطفو · md flush right-0 full-height · الخريطة كاملة على lg).

## م8.8 — حزمة جرافيك ووظائف (جوال + شاشات كبيرة · حزمة طلب · 2026-06-20 · مدموجة في main ومدفوعة)
> بناء إنتاجي ناجح + **54/54 اختبار** + typecheck/lint نظيف. صفر مساس بالمنطق/البيانات/الأدوار/RLS. تحقّق حيّ بقياس DOM + لقطات.
- **#1 بطاقات التسمية خارج نينوى — أُلغيت بطلب المستخدم** (حُذف `margin-labels.tsx` وكل ربطه من `investment-map.tsx`؛ نُفِّذت ودُفِعت ثم أُزيلت بالكامل).
- **#2 نبضة الموقع** (`globals.css` + حلقة التتبّع): **جبهة موجية سونار بطيئة** — خمس حلقات CSS متطابقة متتابعة (حلقة كل ثانية على دورة 5ث · تأخير سالب فلا ومضة تزامن) تزحف معاً للخارج ببطء فدائماً خمس حلقات متّحدة المركز بأنصاف أقطار مختلفة (الجديدة تنبثق قبل تلاشي القديمة بكثير — لا انتظار)، العمق من تدرّج العتمة (الأبعد أخفت)، تباطؤ `cubic-bezier(0.25,0.46,0.45,0.94)`، والعتمة تبلغ 0 عند أقصى اتّساع (×3.1 · بلا مسافة ميّتة)؛ `transform/opacity` فقط؛ `prefers-reduced-motion`؛ cap 50. (تنقيح م8.8.2 — حُسّنت من حلقتين بطلب «أكثر سلاسة ونبضات متتالية»، ثم أُبطئت ووُسّعت إلى خمس متتابعة بطلب «أبطأ وتتابع بلا انتظار».) **م8.8.3:** حجم مرن مع الزوم (`pingScale` smoothstep بين z8.5→12.5، معامل 0.4→1، يُطبَّق `transform:scale` على حاوية كل دبوس) لتفادي التداخل عند الإبعاد؛ ورفع لوحة deck (القطع+الإشارات) إلى `z-index:10` فوق طبقة النبضات (`z-9`) فتظهر النبضات **تحت الدبابيس** (التسميات 12 · الارتفاع 13 · الأدوات 20 تبقى فوق)؛ وتعزيز الحدّة (حدّ 2→2.25 · توهّج 8→10 · ذروة عتمة 0.52→0.6). متحقَّق حيّاً: deck z=10/ping z=9، معامل الحجم 0.4 (محافظة)→0.58 (تقريب).
- **#3 بحث الهيدبار مضمَّن** (`headbar-search.tsx`): حقل حيّ + قائمة منسدلة بلا نافذة (أُزيل `SearchOverlay` من page.tsx)، `Ctrl K` يركّز، الهيدبار رُفع z-50 (فوق السايدبار).
- **#4 صوت الطيران فقط** (`sfx-events.tsx`): بوّابة `data-sfx=off` على أزرار الطيران (كامل نينوى الجوال/الديسكتوب · الموقع المباشر · شارة الدبوس · نتائج البحث) — يُسمَع السووش فقط.
- **#5 بطاقة صور الجوال** (`selected-parcel-card.tsx`): حجم افتراضي أصغر (296×330) + زرّ تصغير/تكبير (يُغذّي الخط القائد) + z فوق الورقة/الدوك (جوال فقط).
- **#6 «كامل نينوى» قرص 3D على الحاسوب (lg)** (`app-sidebar.tsx`) + تحسين أزرار الزوم (≥768px زجاج كحلي 40px · `globals.css`)؛ الريست المسطّح صار md-فقط.
- **#7 إلغاء زر «!»** (`investment-map.tsx`): `attributionControl:false` + نسب مفتوح بلا زرّ (صون الترخيص)؛ أُزيل `ScaleControl` (استبدله مؤشّر #8).
- **#8 مؤشّر «الارتفاع الجوي»** (`investment-map.tsx`): ارتفاع الكاميرا محسوب حتمياً (هندسة ميركاتور: cameraToCenter×m/px — `getFreeCameraOptions` غير متاح في MapLibre)، رقم 3D عائم أسفل-يمين يتحدّث لحظياً، متجاوب بلا تداخل.
- **#9 coverflow → الخيار (ج)** (`mobile-section-sheet.tsx`): قائمة نظيفة ثابتة بـscroll-snap لطيف فقط بلا تكبير/تعتيم (إزالة تأثير الإبراز كلياً).

> **تنقيحات لاحقة (2026-06-21):** م8.8.1 — إلغاء «بطاقات التسمية خارج نينوى» (#1) بالكامل بطلب المستخدم (حذف `margin-labels.tsx` وكل ربطه). م8.8.2 — تحسين نبضة الموقع (#2) إلى جبهة موجية سونار بطيئة سلسة (خمس حلقات متتابعة بلا انتظار). م8.8.3 — حجم النبضة مرن مع الزوم + إظهارها تحت الدبابيس (رفع لوحة deck فوق طبقتها) + تعزيز حدّتها. الثلاثة: typecheck/lint/بناء نظيف + 54/54 اختبار + تحقّق حيّ (قياس DOM للحركة والحجم والترتيب + لقطات على الخريطة الفعلية).

## م8.9 — حزمة تحسينات الجوال + عام (حزمة طلب · 2026-06-21 · فرع `m8.9-mobile-batch`)
> بناء إنتاجي ناجح + **54/54 اختبار** + typecheck/lint نظيف. صفر مساس بالمنطق/البيانات/الأدوار/RLS. تحقّق حيّ بقياس DOM + لقطات (منفذ dev على شاشة جوال 375×812).
- **#1 KPI الجوال أقل شفافية** (`mobile-kpis.tsx`): خلفية القرص `white/0.05→0.12` + حدّ/بريق أقوى + `backdrop-blur-md` — تبرز الأرقام فوق حركة الخريطة وألوانها.
- **#2 تداخل الارتفاع الجوي/البحث** (`investment-map.tsx`): على الجوال نُقل المؤشّر للركن **السفلي الأيسر** `bottom-[calc(var(--sab)+5.5rem)]` (المساحة التي أخلاها الزوم) فوق شريط البحث؛ الديسكتوب يبقى يميناً.
- **#3 ورقة الفرص/الرخص** (`mobile-section-sheet.tsx` + `panel-state.ts` + اللوحتان): (أ) **إزالة scroll-snap** ← زخم حرّ كقسم الشركات؛ (ب) **حشو سفلي 3.5rem** ← آخر بطاقة تظهر (كانت تنحجب)؛ (ج) **تخزين في ذاكرة الوحدة** (`usePersistentState`/`usePersistentScroll`): الفلاتر/البحث/التمرير تصمد عبر فتح/إغلاق التاب وتُصفَّر عند إعادة تحميل الصفحة فقط (= دخول/خروج النظام)؛ التاب الفرعي للرخص يصمد أصلاً عبر `app-sidebar`.
- **#4 فلتر الحي زرّاً عائماً** (`investment-map.tsx`): نُقل من لوحة الطبقات إلى **زر مستقل تحت زر الصوت** (`MapPinned`) يفتح منسدلة فيها حقل بحث + قائمة طولية قابلة للتمرير — كل الشاشات؛ متبادل الإخفاء مع لوحة الطبقات والرسم.
- **#5 الزوم تحت «كامل نينوى» (جوال)** (`mobile-dock.tsx` + `map-nav-store.ts` `requestZoom/onZoom` + `globals.css` + `investment-map.tsx`): أُخفي زوم MapLibre على الجوال (`display:none`) وأُضيف زرّا زوم زجاجيان تحت قرص «كامل نينوى» (الوضع العمودي)، ورُفع الدوك `top-1/2→top-[44%]`. متحقَّق: الزوم يحرّك الكاميرا (2268→567→1134كم) وصفر تداخل.
- **#6 نسيج الزمكان أبرز وأنيق** (`spacetime-wave.ts`): رفع عتامة الزوم (0.62→0.78…) + ألفا الخطوط (0.34→0.42) + السعة (cell×1.5→×1.8) + إبطاء الحركة ~12% (أنعم) — مات بلا بياض حادّ.
- **#7 الحلقات تحت الدبابيس** — تأكيد متانة م8.8.3 (deck `z-10` فوق ping `z-9`) بعد كل تغييرات م8.9 (متحقَّق: 80 نبضة، الترتيب صامد).
- **#8 سحب بطاقة القطعة** (`selected-parcel-card.tsx`): سحب حرّ من **رأس البطاقة** (pointer capture + delta) مقيَّداً داخل حدود الخريطة (لا تخرج عن الشاشة)؛ الخطّ القائد يبقى موصولاً (يستهلك نفس `cardX/cardY`)؛ الأزرار توقف الانتشار. كل الشاشات. (typecheck محقَّق؛ السحب الحيّ يُختبر في الاستخدام — تعذّر تشغيل اختيار deck.gl ببرمجة في المعاينة.)
- **#9 حدّ ارتفاع الورقة** (`mobile-section-sheet.tsx` + `mobile-kpis.tsx`): سقف `expanded` يحسب لحظياً من أسفل الشريط العلوي (KPI، مقيس بـ`data-kpibar`) فيستقرّ شريط التابات أسفله بفجوة **8px** مطابقة تماماً للفجوة بينه والورقة — لا يدخل تحت الهيدبار. متحقَّق: عند الأقصى قمة الشريط 8px أسفل KPI.

## م8.10 — حزمة تجربة المستخدم (٨ تحسينات · حزمة طلب · 2026-06-21 · فرع `m8.10-ux-batch`)
> بناء إنتاجي ناجح + **54/54 اختبار** + typecheck/lint نظيف. صفر مساس بالمنطق/البيانات/الأدوار/RLS. تحقّق حيّ بقياس DOM + لقطات (شاشة جوال 375×812) + **مراجعة عدائية متعدّدة الوكلاء** (5 مراجعين → تحقّق) أمسكت بخللين أُصلِحا.
- **A · KPI كحلي في المود الفاتح** (`map-base-store.ts` جديد + `mobile-kpis.tsx` + `investment-map.tsx`): مخزن `useSyncExternalStore` للقاعدة المرئية؛ `switchBase` يكتبه؛ `mobile-kpis` يقرأه → الخريطة الفاتحة: قرص **كحلي ممتلئ** بأرقام فاتحة؛ الداكن/القمر: زجاج أبيض كما هو. متحقَّق حيّاً.
- **B · إصلاح الطيران عند الورقة الكاملة** (`investment-map.tsx`): `framePadding(base, ignoreSheet)` — `onFlyTo`/`onFlyToCoords` تتجاهل ارتفاع الورقة (تُغلق مع الطيران) فلا يتجاوز الحشو الرأسي ارتفاع الخريطة (كان `fitBounds` يفشل عند الورقة القصوى — يعمل الصوت فقط).
- **C · منسدلة بنقرتين بلا كيبورد فوري** (`filter-combo.tsx` — كل منسدلات النظام): النقرة الأولى تفتح المنسدلة فقط (`readOnly`+`inputMode=none` ← لا كيبورد)، الثانية وسط الحقل تحرّر+كيبورد (`flushSync`+blur+focus داخل إيماءة المستخدم)، السهم زر إغلاق. blur عند النقر خارج الحقل (إصلاح مراجعة). متحقَّق: tap1 readOnly+open، tap2 editable.
- **D · نغمة الزوم** (`mobile-dock.tsx`): إزالة `data-sfx=off` عن زرّي الزوم فتعمل نغمة النقر.
- **E · نتائج البحث تتمدد للأعلى** (`mobile-search.tsx`): النتائج تنبثق من **أسفل بمحاذاة الحقل وتنمو للأعلى** بقدرها، مقيَّدة بـ`max-height` تحت الهيدبار ثم سكرول. الحاوية بحجم `--app-h` (`justify-end`) عند التفعيل فيبقى الحقل فوق الكيبورد (إصلاح مراجعة — كان `bottom-0` يضعه خلف الكيبورد على iOS).
- **F · أرضية كحلية أدكن + معالم بارزة** (`investment-map.tsx buildStyle` + `map-config.ts`): الأرضية كحلية **أدكن** (`NAVY` → `#010308` بطلب «أدكن أكثر») ومضلّعات المعالم تُغمَّق `darken 0.2`، بينما **الطرق `+0.46` والتسميات `+0.52` تبقى بارزة** → أرضية داكنة جداً تتميَّز فوقها القطع/الدبابيس/النبضات والطرق والتسميات بوضوح مريح. متحقَّق حيّاً (أرضية أدكن، شوارع/تسميات أنصع).
- **G · بطاقة اسم القطعة شفافة فوق الدبوس** (`investment-map.tsx` + `globals.css`): بطلب المستخدم رُجِع عن الوضع الجانبي إلى **بطاقة شفافة صغيرة فوق رأس الدبوس** (`bg navy/0.42` + `backdrop-blur`، نصّ فاتح، `text-8.5px`) تظهر **بحركة سلسة** (`@keyframes pin-label-in`) وتتبع الدبوس وتتلاشى بالإبعاد؛ حُذف `layoutPinLabels`/الخطوط الجانبية. متحقَّق: البطاقة فوق الدبوس · شفافة · صغيرة.
- **H · تخزين التاب الفرعي للرخص** (`app-sidebar.tsx` + `panel-state.ts`): `licenseStatus` عبر `usePersistentState("lic:status")` + **إزالة تصفيره عند الفتح** (كان السطر يصفّر «منجزة/مسحوبة» عمداً) — يصمد عبر الفتح/الإغلاق، يُصفَّر عند دخول/خروج النظام. (مكمّل لتخزين الفلاتر في م8.9.)

## م8.11 — تحسينات بصرية للخريطة (٥ تعديلات · حزمة طلب · 2026-06-21 · فرع `m8.11-map-visuals`)
> typecheck/lint نظيف + **54/54 اختبار** + بناء إنتاجي نظيف + تحقّق حيّ بقياس DOM ولقطات.
- **إلغاء نسيج الزمكان** (`investment-map.tsx` + حذف `spacetime-wave.ts`): أُزيلت طبقة WebGL وكل ربطها (الاستيراد + الاستدعاءان + `applySpacetime`) بطلب المستخدم.
- **تعتيم داخل نينوى** (`investment-map.tsx overlayLayers`): طبقة `inside-dim` (`rgba(1,3,8,0.5)` فوق مضلّع المحافظة) → الأرضية داكنة كالخارج وأكثر؛ أرضية هادئة تبرز فوقها العناصر/القطع/النبضات.
- **بطاقة اسم القطعة أعلى الدبوس** (`investment-map.tsx`): رُفِعت `y-30 → y-54` فلا تغطّي الدبوس (فجوة ~54px فوق المرساة · متحقَّق).
- **شدة لونية أكبر للنبضات** (`globals.css`): حدّ `2.25→2.5` · توهّج `10→14px` · ذروة عتمة `0.6→0.8` → ألوان الحالات أبرز (متحقَّق: توهّج 14px بلون الحالة).
- **توهّج نابض هادئ لحدود نينوى** (`investment-map.tsx` + `overlayLayers`): طبقة `bnd-governorate-glow` (line-blur 5) تُحرَّك `line-opacity` بـrAF (sine `0.16..0.5` · نبض ~6ث · مُقَلّم ~25fps للبطارية) — يعلو ويخفت بسلاسة. متحقَّق: السطوع يتغيّر بين إطارين بفارق 3.5ث.

## م8.12 — معالم الخريطة المضيئة + تبديل 3D/2D (تعديلان · حزمة طلب · 2026-06-21 · فرع `m8.12-map-features`)
> typecheck/lint نظيف + **54/54 اختبار** + بناء إنتاجي نظيف + تحقّق حيّ بلقطات (مستوى المحافظة + مستوى المدينة).
- **تعزيز معالم الخريطة على أرضية داكنة** (`investment-map.tsx` حلقة إعادة التلوين + `overlayLayers`): بطلب المستخدم «اللون الداكن جيد… عزِّز لون الشوارع والحدود والمباني والأنهار وكل الخطوط المهمّة وامنحها فتحة بتوهّج خفيف جداً وابقِ الأرضية فقط داكنة». **أُلغيت طبقة `inside-dim`** (م8.11) لأنها كانت تُعتِم المعالم المضيئة. إعادة تلوين حسب نوع الطبقة في النمط الداكن: خطوط `line` → `lightenColor 0.58` + `line-blur 0.6` (توهّج خفيف) · رموز `symbol` → نصّ `lighten 0.55` وهالة بلون الأرضية · `fill-extrusion` (المباني ثلاثية الأبعاد) → `lighten 0.32` · تعبئة `fill`: ماء `lighten 0.24` (أنهار زرقاء بارزة) · مبانٍ `lighten 0.3` · بقيّة الأرضية `darkenColor 0.34` (تبقى داكنة). النتيجة المتحقَّقة: شوارع/أنهار/مبانٍ مضيئة فوق أرضية داكنة — على مستوى المدينة نهر دجلة أزرق بارز وشبكة شوارع مضيئة.
- **زرّ تبديل عرض الخريطة 3D/2D** (`investment-map.tsx`): زرّ زجاجي **تحت زرّ فلتر الحي** (كل الشاشات) — `pitch: 48` في المُنشئ (**الافتراضي 3D عند البدء**) + حالة `map3D` + `easeTo({ pitch: next ? 48 : 0, duration: 600 })` عند النقر، والتسمية تتبدّل `3D ⇄ 2D`. (fitBounds/flyTo تُبقي الـpitch الحالي ما لم يُحدَّد، فلا يكسر التنقّل العرض المائل.) متحقَّق حيّ: النقر يسطّح الخريطة إلى عرض علوي والتسمية تصبح «2D».

### م8.12.1 — انبساط حلقات النبضة على الأرض في العرض ثلاثي الأبعاد (`investment-map.tsx`)
بطلب المستخدم: في 3D كانت حلقات النبضة (السونار) تبقى **دوائر عمودية عائمة** مواجِهة للكاميرا (billboard) لا تتغيّر كما في 2D؛ المطلوب أن **تنبسط على الأرض كأنها جزء من الرسمة**. الحلّ (الحلقات دوائر متماثلة شعاعياً فالاتجاه/البيرنغ لا يؤثّر، يكفي ميل الكاميرا):
- حالة `pingTilt = { deg, persp }` تُحدَّث داخل حلقة `compute` الحيّة (rAF على `move`/`zoom`): `deg = map.getPitch()` (مقرّب 0.1) · `persp = (0.5/tan(fov/2))·H ≈ 1.5·H` (مسافة الكاميرا بالبكسل) — مع حارس مقارنة يمنع إعادة الرسم بلا تغيّر.
- حاوية النبضات تأخذ `style={{ perspective }}` (مركز المنظور = مركز الشاشة = نقطة الكاميرا الرئيسة)، وكل غلاف دبوس يأخذ `transform: rotateX(${deg}deg) scale(${pingScale})` حول `transform-origin: 0 0` (نقطة الدبوس).
- النتيجة: في 3D تنبسط الحلقات على الأرض (إهليلج متوقّع التقصير يحتضن الأرض) والدبوس (deck.gl billboard مرساته القاع) يقف منتصباً في مركزها؛ في 2D (ميل 0) `rotateX(0)` محايد فتبقى دوائر كما كانت (لا تغيّر).
- متحقَّق حيّ (جوال 375×812): مصفوفة الغلاف عند ميل 48 = `rotateX(48°)·scale` بالضبط · perspective 1131px · عند ميل 0 = مصفوفة وحدة · لقطتان تُظهران الإهليلج (3D) والدائرة (2D) · بلا أخطاء كونسول.

## م9 — الخارطة الاستثمارية (مخطّطة · الخطة المعتمدة للبناء القادم)
> **الخطة التفصيلية الكاملة + بروتوكول الاستئناف عبر الجلسات:** `docs/م9_الخارطة_الاستثمارية.md` (**تُقرأ بالكامل قبل بناء أي مرحلة من م9**). فهرسها في `docs/خريطة_البناء.md`.
- سطح «الخارطة الاستثمارية» (إعادة تسمية «تصميم فرصة») للفرص **المفترضة**: مجسّم 3D هولوكرامي يرفعه المستخدم (**glb + STL من سكتشب ويب** · `ScenegraphLayer`) + بطاقات تنبثق من المجسّم تعيد استخدام محرّكات النظام (ضوابط §ج.9 · مشاريع ممكنة 🟩 · صور · بيانات · جاهزية حتمية) + **جولة تلقائية** (وضع كشك) بفلترة قطاع.
- صفر تأليف (المجسّم «تصوّر مبدئي») · §ج.9 حتمي · الأدوار بـ`is_admin()` · process-line الموافقات مؤجَّل (م9.x).
- **الحالة:** قيد البناء على فرع `m9-investment-map` (المراحل م9.0→م9.6، حالتها المحدَّثة في ملف الخطة §م).

### م9.0 — التهيئة والتسمية ✓ (2026-06-23)
- **إعادة تسمية** القسم «تصميم فرصة» → **«الخارطة الاستثمارية»** (المعرّف `opportunity-design` ثابت لتفادي أي كسر): `sections.ts` (label «الخارطة الاستثمارية» · short «الخارطة») + نصّان مستخدمان (`parcel-window.tsx` تسمية حالة المفترضة · `investment-map.tsx` نصّ حذف الحدود) + تعليق `app-sidebar.tsx`.
- **تبعيات 3D (محاذاة دقيقة):** `@deck.gl/mesh-layers@9.3.3` (ScenegraphLayer) + `@loaders.gl/{core,gltf,draco}@4.4.2` — **loaders.gl 4.x لا 9.x** (deck 9.3.3 يستعمل loaders 4.4.2؛ صُحِّح خطأ المسح «^9.3»). لم تُستورَد بعد (تُستعمَل في م9.3) — صفر تغيير سلوكي.
- **مؤجَّل لـم9.3:** إعداد Draco worker محلي في `public/vendor` + حسم مسار STL (تحويل STL→glb أو عارض STL) عند أوّل تحميل.
- **القبول ✓:** typecheck + lint نظيف + **بناء إنتاجي نظيف**؛ القسم مُعاد تسميته بلا أي تغيير في المنطق/البيانات/الأدوار.

### م9.1 — البيانات والتخزين ✓ (الكود + dry-run · تطبيق الهجرة بانتظار إذن المستخدم · 2026-06-23)
- **هجرة `20260613120033_parcel_models.sql` (إضافية بالكامل):** دلو `parcel-models` (خاص) + سياسات تخزين (قراءة للمصادَقين · **رفع/حذف للمدير `is_admin()`**) + جدول `parcel_models` (id · kind · ref_id · storage_path · format[glb|gltf|stl] · `transform` jsonb · title · is_conceptual · uploaded_by · طوابع · فهرس kind/ref_id) + RLS (قراءة للكل · كتابة للمدير) + إضافته لمنشور الزمن الحقيقي (idempotent) + **`move_parcel` v6** (سطر cascade واحد يرحّل المجسّم مع القطعة عند النقل، مع الصور/الرؤى).
- **`model-lib.ts`** (`features/parcels/models/`): `uploadParcelModel`/`updateParcelModel`/`deleteParcelModel`/`signedUrlsForModels`/`useParcelModels` — نمط `photo-lib`، تحقّق **بالامتداد** (glb/gltf/stl · MIME الـ3D غير موثوق) · سقف 40MB · حدّ 4 نماذج · النموذج «تصوّر مبدئي» (`is_conceptual` افتراضي true) · `transform={scale,rotationDeg,elevationM,anchorOverride?}`.
- **`realtime.ts`:** `parcel_models` ضمن `TABLES` + إبطال `["parcel_models"]`.
- **القبول:** typecheck + lint نظيف + **dry-run ناجح** (الهجرة صحيحة، تراجَعت بلا تثبيت). **`model-lib` غير مستورَد بعد** (يُستعمَل في م9.2) — صفر تغيير سلوكي.
- ✓ **طُبِّقت الهجرة على الإنتاج** (`npm run migrate` · COMMIT · 2026-06-23، بإذن المستخدم الصريح «نفّذ الهجرة») — جدول `parcel_models` + الدلو + RLS + منشور الزمن الحقيقي + `move_parcel` v6 أحياء على Supabase.

### م9.3أ — العرض الهولوكرامي بالكتلة الإجرائية ✓ **متحقَّق بصرياً** (إعادة ترتيب · 2026-06-23)
> **سبب إعادة الترتيب:** م9.2 (رفع glb) يحتاج الجدول الإنتاجي؛ ريثما يُؤذَن بالهجرة، نفّذنا **بديل العرض بلا قاعدة** (الكتلة من الحدود) لإثبات قدرة المجسّم 3D حيّاً بلا تبعية قاعدة.
- طبقة `parcel-massing` في `parcelLayers` (`investment-map.tsx`): `GeoJsonLayer extruded` **لكل القطع المفترضة دائماً** (لا تعتمد على التحديد/التركيز فلا تختفي بالنقر خارجها — طلب المستخدم) — كتلة من حدود القطعة بارتفاع تصوّري 60م، **تعبئة كثيفة** (`alpha 232` كحالة التحديد · اشتداد غمق اللون) بنفس **مادة/لون أرض القطعة** (`fillRgba('assumed')` + `material:false` مسطّح) + إطار سلكي بلون الحدّ. بديل يُستبدَل بنموذج glb عند رفعه (م9.3ب).
- **دمج الرسمة بالكتلة** (طلب المستخدم): تعبئة أرض المفترضة المسطّحة صارت **كثيفة دائماً** (`alpha 205→235`) بنفس لون الكتلة، فيبدو الكيان امتداداً صلباً من الأرض لا كتلة شفّافة عائمة فوقها.
- **حالة تركيز مستقلّة `modelFocusId`** (منفصلة عن التحديد/البطاقة، لا تتحكّم بظهور الكتلة): تُضبَط عند الطيران/النقر، تُصفَّر عند كامل نينوى/نقر الفراغ — تخدم الحركة السينمائية وبطاقات م9.4 لاحقاً.
- **الطيران لا يفتح البطاقة** (طلب معتمد): يعرض الكيان الهولوكرامي فقط. النقر المباشر يفتح البطاقة كالمعتاد.
- **حركة سينمائية (طلب المستخدم):** عند الطيران لقطعة مفترضة — **(1) اقتراب قريب ثلاثي الأبعاد** (`flyTo` pitch 60 · curve 1.5 · ~2.2ث) ثم **(2) التفاف «درون» نصف دائرة** (`easeTo` bearing +180° · pitch 60 · ease-in-out · ~5ث) ثم توقّف بسلاسة. يُلغى الالتفاف إن غادر المستخدم التركيز · يحترم `prefers-reduced-motion` (اقتراب قصير بلا التفاف). **بدائية التركيز هذه يحتاجها التجوّل التلقائي م9.5.**
- لا تبعيات/قنوات جديدة.
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً حيّاً** (جوال 414×860): الطيران لقطعة «العمريني» أظهر **كتلة بنفسجية صلبة كثيفة ثلاثية الأبعاد** (نفس مادة/لون الأرض، كثيفة كحالة التحديد) فوراً بلا فتح بطاقة (`cardOpen=false`)، كيان دائم لا يختفي بالنقر خارجه (مستقلّ عن التحديد بنيوياً)، مع الحركة السينمائية (اقتراب + التفاف درون). HMR حيّ على :3000.

### م9.2 — رفع المجسّم ومحرّر التموضع (للمدير) ✓ الكود (رفع حيّ بيد المستخدم · 2026-06-24)
- **`models-section.tsx`** (`features/parcels/models/`): قسم «المجسّم ثلاثي الأبعاد» في نافذة القطعة المفترضة — **رفع glb/gltf/stl** (سكتشب ويب) + قائمة + حذف + **محرّر تموضع لكل نموذج** (مقياس/دوران°/ارتفاع‑م مع «حفظ التموضع») عبر `model-lib`. يُعرَض **للمفترضة فقط** (`kind==='assumed'`) · للقراءة عند الـviewer (لا رفع/تعديل/حذف) · النموذج مؤشَّر «تصوّر تصميمي مبدئي». نمط مطابق لـ`PhotosSection`.
- **إدماج** في `parcel-window.tsx` بجوار قسم الصور (شرط `kind==='assumed'`). الرفع/الحذف/التعديل مفروض بـRLS (`is_admin`) + إخفاء واجهة مكمّل.
- **القبول:** typecheck + lint نظيف. القسم مُدمَج ومطابق لنمط الصور المثبَّت. **الرفع الفعلي يختبره المستخدم بملفّاته** (إدخال الملف لا يُحاكى بأداة المعاينة). **عرض النموذج على الخريطة (محلّ الكتلة) = م9.3ب** (التالي).

### م9.3ب — عرض النموذج المرفوع على الخريطة ✓ **متحقَّق بصرياً** (2026-06-24)
- **`model-render.ts`** (`features/map/lib/`): `registerModelLoaders` (GLTFLoader + DracoWorkerLoader) · **`parseBinaryStl`** (محلِّل STL ثنائي — لا محمّل STL في loaders.gl) · **`buildModelLayers`**: glb/gltf → `ScenegraphLayer` (تحميل ذاتي للرابط · `_lighting:'pbr'` · roll 90 لتقويم محور Y) ، stl → `SimpleMeshLayer` بشبكة محلَّلة (`material:false` هولوكرامي · لون الحالة). مرسىً عند مركز القطعة بتحويل (مقياس=`sizeScale` · دوران=`yaw` · ارتفاع=z).
- **`useAssumedModels`** (model-lib): كل نماذج المفترضة (أحدث نموذج لكل قطعة) + روابط موقّعة.
- **إدماج في `investment-map`:** حالة `assumedModels` + تحميل/تحليل شبكات STL (effect) + تسجيل المحمّلات؛ حلقة الطبقات تدمج `buildModelLayers` و**تستثني القطع ذات النموذج من الكتلة الإجرائية** (النموذج الحقيقي يحلّ محلّها).
- **إصلاح حرج (تحقّق المستخدم 2026-06-24):** كانت شبكة STL تُمرَّر لـ`SimpleMeshLayer` بصيغة `{positions, normals}` (مصفوفات مباشرة) → deck 9 يتعطّل في `normalizeGeometryAttributes` (يقرأ `.value.length`) فيُسقط **حلقة عرض deck بأكملها** (الخريطة معطّلة · النموذج لا يظهر). **العلاج:** صيغة `mesh:{ attributes:{ positions:{value,size:3}, normals:{value,size:3} } }`. + **إضاءة `LightingEffect`** (محيطة 1.6 + اتجاهيّتان) على المنصّة كي تظهر نماذج pbr فوق الأرضية الداكنة بدل أن تُعتِم. (سكربت تشخيص `scripts/check-model.ts`.)
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً حيّاً بعد الإصلاح**: المكعّب STL (50×60م) يظهر **ثلاثي الأبعاد مضاءً على «مبنى تجاري تسوقي» محلّ الكتلة** (لقطة محفوظة). **رفع المستخدم glb (Cottage) نجح** (صفّان مؤكَّدان · جلب 200). النماذج الصغيرة (ككوخ) تحتاج **مقياساً أكبر** في محرّر التموضع لتبدو بحجم مبنى. **الضبط المتقدّم (مقياس تلقائي/اتجاه/طابع هولوكرامي) = م9.7.**

## م9.7 — تصميم الكيان الثلاثي المتقدّم (قيد البناء · قطاع السكني أولاً)
> **قرارات المستخدم:** برج **بارامتري إجرائي** يُبنى بجهد فائق ليكون واقعياً (مخوّل بحث الشبكة) · اللون → **أزرق رقمي (AI) `#22C3F3`** على **حالة المفترضة عموماً** (مع إبراز النبضات/تدرّجات/ظلال لتجربة 3D مبهرة).
> **رؤية العرض (السكني):** الرسمة = أرضية المشروع الزرقاء الهولوكرامية · منسدلة اختيار المجسّم + **عدد** (1/N) + تباعد/توزيع · عدد=1 يتمركز وتنطلق النبضات من مركزه لتملأ حدود الرسمة · عدد=N توزيع تلقائي داخل البصمة بملاءمة بصرية · البرج يندمج بالقاعدة (حقيقي + هولوكرامي) · ثابت دائم جزء من الخريطة 3D.

### م9.7.1أ — الهوية اللونية: أزرق رقمي (AI) ✓ متحقَّق بصرياً (2026-06-24)
- **لون حالة «مفترضة» عموماً:** `#8B6FB0` (بنفسجي) → **`#22C3F3` (أزرق سماوي رقمي متوهّج)** في كل تعريفات لون الحالة فقط: `tailwind.config` (`state.assumed` → كل `bg/text-state-assumed`) + `parcel-colors` + `parcel-markers` + `PING_HEX` + `STATE_HEX` (بطاقة القطعة) + `reports-panel` + PDF (`STATE_COLOR` + شريط الحالات). **لم تُمسّ** تدرّجات/توهّجات الواجهة العامّة (`rgba(139,111,176)` بقيت — ليست لون حالة بل لمسة هولوكرامية عامّة؛ صُحِّح تجاوزٌ أوّلي بـsed عمّ الواجهة فأُعيد قصره على لون الحالة فقط).
- **القبول ✓:** typecheck نظيف · **متحقَّق بصرياً**: «مبنى تجاري تسوقي» صار **أرضية هولوكرامية زرقاء رقمية متوهّجة** (الرسمة = أرضية المشروع) · نقطة «مفترضة» = `rgb(34,195,243)`. (لقطة محفوظة.)
- **التالي (م9.7.1):** ب) مقياس تلقائي (ملاءمة النموذج للبصمة) · ج) برج سكني بارامتري واقعي · د) منسدلة المجسّم + العدد + التوزيع · هـ) النبضات من مركز المجسّم تملأ الرسمة · و) اندماج بصري + تحقّق.

### م9.7.1ج — البرج السكني البارامتري الواقعي (هندسة إجرائية) ✓ متحقَّق بصرياً (2026-06-24)
- **الفكرة:** كل قطعة مفترضة **لا نموذجَ مرفوع لها** تُولَّد لها هندسة برج إجرائياً من بصمتها — لا حاجة لاستيراد. النموذج يقرأ ككتلة معماريّة واقعيّة على القمر الصناعي.
- **`parametric-tower.ts` (جديد):** `generateTower(w,d)` يُخرِج شبكتين Float32Array (`body` + `windows`). كتلة ثلاثية المراحل: **بوديوم** (×1.08 أعرض) + **جذع** + **تتويج** (×0.66 أضيق) عبر `pushBox/pushQuad`. ملاءمة تلقائية: `totalH=clamp(28..110, max(w,d)×6)`، الطوابق `=(shaftTop−PODIUM_H)/FLOOR_H`. **شبكة نوافذ** منبعثة (أعمدة×طوابق) على الواجهات الأربع، بارزة 0.15م.
- **`model-render.ts`:** `buildTowerLayers(items)` — طبقتا `SimpleMeshLayer` لكل برج: **الجسم** `[12,52,96]` أزرق غامق `material:true` (مضاء بـ`LightingEffect` فيقرأ صلباً ثلاثياً، **أغمق من الحلقات `#0E5FB0`**) + **النوافذ** `[120,225,255]` سماوي `material:false` (منبعث متوهّج). صيغة الشبكة المصحَّحة `{attributes:{positions:{value,size:3},normals:{value,size:3}}}`.
- **`investment-map.tsx`:** `towerCacheRef` (خبء الشبكة بالمعرّف، تُولَّد مرّة) · في effect الطبقات: لكل قطعة مفترضة بلا نموذج، بصمتها بالمتر من `bbox` (×0.55 هامش ملاءمة داخل القطعة)، تُولَّد عند المركز · `towerRefIds` تُمرَّر لـ`parcelLayers` ل**استثناء البرج من الكتلة الإجرائية** (يحلّ محلّها كالنموذج المرفوع) · الدمج `[...parcelLayers, ...buildModelLayers, ...buildTowerLayers]`.
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً** (قمر صناعي · «مبنى تجاري تسوقي»): برج أزرق ثلاثي المراحل (بوديوم/جذع/تتويج) بشبكة نوافذ سماوية متوهّجة، قائم على الأرضية الهولوكرامية الزرقاء، فوق الحلقة وأغمق منها (لقطة محفوظة). صفر أخطاء console.

### م9.7.1هـ — حلقات أرضية متوهّجة بارزة تنبعث من مركز البرج (deck) ✓ متحقَّق بصرياً (2026-06-24)
- **المشكلة المكتشَفة عند المعاينة:** الأرضية الهولوكرامية الصلبة (تُرسَم في deck · z-10) كانت **تغطّي حلقات النبضة (DOM · z-9 تحتها)** فلا تبرز تحت البرج. الحلّ: حلقات أرضية تُرسَم **في deck** فوق الأرضية وتحت البرج.
- **`generateGroundRings(maxRadius, count=4)` ([parametric-tower.ts]):** شبكة حلقات (annuli) مسطّحة على مستوى الأرض (`z=0.6` يتفادى تنازع العمق)، `count` حلقات متّحدة المركز بفواصل منتظمة من المركز حتى `maxRadius` — تنبعث من مركز البرج وتملأ القطعة.
- **`buildTowerLayers` ([model-render.ts]):** طبقة `SimpleMeshLayer` للحلقات **قبل** جسم البرج (فتُرسَم تحته)، لون `[40,150,240]` أزرق متوهّج `material:false` (منبعث) — **أفتح من جسم البرج `[12,52,96]`** ⇒ الجسم أغمق منها كما طُلب.
- **`investment-map.tsx`:** لكل برج تُولَّد حلقاته `generateGroundRings(0.5·min(wM,dM)·0.9, 4)` (داخل حدود القطعة)، تُخبّأ مع شبكة البرج `{tower, rings}` بالمعرّف، وتُمرَّر في `TowerItem.rings`.
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً** (قمر صناعي · «مبنى تجاري تسوقي» · 1كم): حلقات زرقاء متوهّجة متّحدة المركز بارزة على الأرض حول قاعدة البرج، الأرضية الهولوكرامية محفوظة، البرج فوقها وأغمق منها · صفر أخطاء console. **(الحلقات ساكنة حالياً — تحريك النبض المنبسط خيار تالٍ.)**

### م9.7.1و — مواءمة واقعية البرج من تصاميم فيكتور (EPS) ✓ متحقَّق بصرياً (2026-06-24)
- **المصدر:** ملفّا EPS من المستخدم (`299.eps` أبراج سكنية على قطع · `13509.eps` «building set» برج زجاجي أزرق) — حُوِّلا محلياً عبر **Ghostscript 10.07.1** (ثُبِّت user-scope بلا صلاحيات: `%LOCALAPPDATA%\Ghostscript\gs10071`) إلى PNG، واستُخرِجت منهما عناصر الواقعية. اللافت: الأزرق العاكس + الحلقات الزرقاء في المرجع تطابق هويتنا الرقمية.
- **إعادة بناء `parametric-tower.ts`:** البرج الآن **أربع شبكات حسب الخامة**: `body` (هيكل مضاء) · `glassA/glassB` (زجاج أزرق منبعث **بنغمتين متناوبتين/طابق** = تفاوت ألواح عاكس) · `accent` (حلقة/خطوط مميِّزة منبعثة). العناصر المُواءَمة: **شُرفات بارزة + حاجز زجاجي/طابق** · **مونتينات عموديّة** على خطوط الأعمدة · **بوديوم + بلِنث أرضي** + شريط زجاج اللوبي · **تتويج** (حلقة مميِّزة + بارابيت + بنتهاوس + خطّ تتويج متوهّج). بناء الواجهة عبر `buildFace` في إطار محليّ ثم `rotateAppend` للأوجه الأربعة (0/90/180/270°). نواة جذع صلبة تمنع الشفافية بين الأوجه.
- **`model-render.ts`:** `buildTowerLayers` يرسم 4 طبقات لكل برج عبر `meshLayer` (الهيكل مضاء `material:true` · الزجاج/الحلقة منبعثة `material:false`). الألوان: `body[16,42,74]` (أغمق من كلّ شيء) · `glassA[72,168,232]` · `glassB[40,108,176]` · `accent[95,212,255]`.
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً** (قمر صناعي · «مبنى تجاري تسوقي» · 715م→1.4كم): واجهة زجاجية ستائريّة زرقاء واقعيّة (مونتين + أحزمة طوابق + تفاوت ألواح) + بوديوم + تتويج بنتهاوس وحلقة مميِّزة + الحلقات الأرضية المتوهّجة · صفر أخطاء console. (الشُرفات قائمة لكنها خفيفة البروز عن بُعد — تعميقها خيار تالٍ.)

### م9.7.1و+ — ترقية مودرن واقعية: خامات متنوّعة + نوافذ مضيئة + ظلال 3D (2026-06-24)
- **بحث تصميم متوازٍ (Workflow `tower-design-research`):** 3 وكلاء Explore (تصميم الهندسة · الخامات والألوان · تقنية ظلال deck.gl) قرؤوا مرجعَي EPS + الكود وأعادوا مواصفات. الخلاصة المُطبَّقة: deck shadows لا تسقط على أساس القمر الصناعي والممرّ العام يخاطر بأداء الإنتاج ⇒ **تظليل الهيكل المضاء (3D حقيقي) + ظلّ تماسٍ مُخبوز** بدل ظلال عامّة.
- **هولوكرامي لكن مُعتِم + ألوان متنوّعة (طلب المستخدم):** الهيكل **فولاذي أزرق-رماديّ مضاء `[46,58,78]`** (تظليل اتجاهيّ = عمق 3D، أغمق من الزجاج) · زجاج بنغمتين `glassA[110,210,245]`/`glassB[35,130,190]` · **نوافذ مضيئة متناثرة** `winCool[188,224,248]` + قليل دافئ `winWarm[255,200,120]` (حياة وتنوّع — تجزئة حتميّة `hash2`) · حلقة/تتويج `accent[100,230,255]`. كلّها مُعتِمة (α=255) فلا تظهر النبضات من خلالها.
- **ظلّ التماس `generateContactShadow`:** قرص داكن شفّاف `[4,10,20,95]` عند `z=0.08` (أسفل الحلقات)، مُزاح عكس اتجاه الشمس = إيهام ظلّ مُلقى. يُولَّد ويُخبّأ مع البرج `{tower,rings,shadow}`.
- **الإضاءة:** محيط `1.6→1.25` + شمس `1.3→1.7` (دافئة قليلاً) = تظليل أوضح على الهيكل.
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً** (قمر صناعي · «مبنى تجاري تسوقي» · 1كم→2كم): برج مودرن بزجاج بنغمتين + نوافذ سماوية/دافئة مضيئة متناثرة + تظليل 3D على الهيكل + تتويج + حلقات أرضية + ظلّ تماسٍ خفيف · النبضات تنتشر من القلب نحو الحدود والبرج فوقها مُعتِماً · صفر أخطاء console.

### م9.7.2 — مكتبة نماذج القطاعات (برج/مول/فندق) + مقياس + تحقّق (2026-06-24)
- **نظام أنواع:** `generateModel(kind)` موزّع (`parametric-tower.ts`) — `tower` (مرتفع نحيف بطوابق كثيرة، نكسة علويّة) · `generateMall` (منخفض عريض صلب: دعامات + ستورفرونت مجزّأ + أحزمة لافتات متوهّجة + مدخل أتريوم بمظلّة + سكايلايت + ميكانيكا سطح) · `generateHotel` (بوديوم فخم + بورت-كوشير + برج نزلاء أنيق عبر buildFace + سكاي لاونج وتتويج).
- **لوحات حسب النوع (`model-render.ts` PALETTES):** برج بارد أزرق · مول محايد رماديّ بلافتات سماويّة · **فندق دافئ كريميّ/ذهبيّ** بنوافذ غرف دافئة — تنوّع لونيّ (ليس أزرق موحّداً، طلب المستخدم).
- **المقياس:** البرج `clamp(64..150م، ref×5.5)` نحيف مرتفع (بعد أن طلب المستخدم تكبيره) · المول يملأ القطعة (بصمة ≤150×130) لكنه منخفض ~20م.
- **التعيين المؤقّت:** نوع كل قطعة مفترضة من اسمها (`tempKindFor`: مول/فندق/برج) ريثما تُبنى المنسدلة (م9.7.1ب/د). `FORCE_KIND` أداة تحقّق فقط (=null).
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً** (قمر صناعي، فرض كل نوع على «مبنى تجاري تسوقي» المؤطَّر جيداً): البرج المرتفع · المول المنخفض العريض · الفندق الدافئ الفاخر — صفر أخطاء console.
- **معالجة تشغيل:** عَلِق مراقِب `next dev` عن التقاط التعديلات (يخدم نسخة قديمة) → حُلَّ بإعادة تشغيل الخادم. **المتبقّي من طلب المستخدم:** ج) تحريك نبض الحلقات · ب) المنسدلة (نوع + عدد 1/N + توزيع، تحتاج هجرة تخزين) · الأعلام (علم العراق + علم الهيئة — **يلزم رفع شعار الهيئة، لا يُؤلَّف**).

### م9.7.1ب — منسدلة اختيار النموذج البارامتري (نوع + عدد + توزيع) ✓ متحقَّق بصرياً (2026-06-24)
- **الهجرة (مطبَّقة على الإنتاج COMMIT بإذن صريح):** `20260624120000_parcel_parametric.sql` — جدول `parcel_parametric_models` (kind, ref_id, model_kind ∈ برج/مول/فندق, count 1..24, distribution ∈ شبكة/صفّ/نثر) + RLS (قراءة للمصادَقين · كتابة للمدير) + realtime. إضافيّ بالكامل (لا مساس بـparcel_models ولا move_parcel).
- **طبقة البيانات (`model-lib.ts`):** `upsertParcelParametric` / `clearParcelParametric` / `useParcelParametric` / `useAssumedParametric` (Map<refId,إعداد> للخريطة).
- **الواجهة (`parametric-section.tsx`):** منسدلة النوع (برج/مول/فندق/تلقائي) + العدد + التوزيع (للمدير)، مدمجة في نافذة القطعة المفترضة تحت قسم رفع المجسّم. «تلقائي» = حذف الإعداد ⇒ تخمين من اسم القطعة.
- **العرض (`investment-map.tsx`):** يقرأ الإعداد بدل التخمين؛ `count=1` عند المركز · `count>1` يوزّع نسخاً على شبكة/صفّ/نثر داخل البصمة بحجم خليّة ملائم (شبكة واحدة تُعاد استخدامها بمواضع متعدّدة، مُخبّأة بمفتاح نوع|عدد|توزيع).
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً** (قمر صناعي): «موول» بإعداد مول×4 شبكة ظهرت **أربعة مولات موزّعة**؛ ثم ضُبِط عرض نظيف (مول/فندق/برج ×1 لكلّ من القطع الثلاث عبر `scripts/set-parametric-demo.ts`). تحذير deps array كان أثر Fast-Refresh فقط (المصفوفة ثابتة الطول 9 في بناء الإنتاج).
- **المتبقّي من طلب المستخدم:** ج) تحريك نبض الحلقات · الأعلام (علم العراق + علم الهيئة — **يلزم رفع شعار الهيئة**).

### م9.7.3 — تطوير متقدّم: نظام المرافق (extras) + تمايز المول بأتريوم ومرافق + الحلقات تحت القاعدة (2026-06-24)
- **بحث موسّع (Workflow `advanced-building-research`):** 4 وكلاء (تصميم مول/فندق/برج + تقنيات ريندر deck.gl) بمواصفات مفصّلة. خلاصة الريندر: SimpleMeshLayer يدعم **texCoords/textures + ألوان رأسيّة (colors) + خامات Phong لكل طبقة + نواظم ناعمة** — فاعتمدنا الاتجاه «المزيج» (تفاصيل/مرافق/خامات دون كساء بعد).
- **نظام المرافق (`parametric-tower.ts`):** `Extra{mesh,color,lit}` + `TowerMeshes.extras[]` (إضافيّ). مكوّنات منحنية/مرافق: `pushQuadVN` (نواظم لكل رأس) · `cylinder`/`cone`/`tree` · `barrelVault` (قبو زجاجيّ ناعم) · `parkingLot` (شبكة بلاطات) · `flatRect` (عشب/ساحة).
- **المول متمايز:** بصمة مبنى (≤115×95) + **قبو زجاجيّ أتريوم متوهّج** على السطح + **مرافق حوله**: باركات أمام/خلف · حدائق جانبيّة · ~12 شجرة. `model-render` يرسم `extras` كلٌّ بلونه/خامته.
- **الحلقات تحت قاعدة المبنى** (طلب المستخدم): نصف القطر = بصمة المبنى ×~0.46 (لا تملأ القطعة) — تلتصق بالقاعدة.
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً** (قمر صناعي · «موول»): مول منخفض عريض بأتريوم زجاجيّ متوهّج + حدائق خضراء + باركات + أشجار + حلقات تحت القاعدة — تمايز واضح. (تحذير deps array = أثر Fast-Refresh، حميد.)
- **المتبقّي:** الفندق والبرج (مرافق/تمايز مماثل) · تحكّم يدويّ بالأبعاد (ارتفاع/طول/عرض) · الأعلام (يلزم شعار الهيئة). **مواصفات البحث محفوظة في مخرجات الـworkflow للجلسة.**

### م9.7.4 — استعراض حرّ للمجسّم (تدوير بكل الاتجاهات + زوم · شبه-سكتشفاب) ✓ متحقَّق بصرياً (2026-06-24)
- **`maxPitch: 85`** في إنشاء الخريطة (ميل أعمق لاستعراض المجسّمات من زوايا منخفضة).
- **وضع الاستعراض الحرّ (`orbitOn`):** زرّ يظهر عند تركيز قطعة مفترضة (`modelFocusId`). عند تفعيله: يتمركز على المجسّم + **اسحب لتدوير الكاميرا حوله** (اتجاه 360° + ميل 0..85° سلس) + **العجلة للزوم حول المركز** — مع تعطيل pan/dragRotate/scrollZoom الافتراضيّة ومعالِجات مؤشّر مخصّصة، وإيقاف الالتفاف التلقائيّ (`m.stop()`). يُنهى تلقائياً عند العودة لكامل نينوى.
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً** (قمر صناعي · «موول»): تفعيل الزرّ ثمّ سحب على اللوحة دوّر الكاميرا وأمالها حول المول مع بقائه متمركزاً. (حدّ المنصّة: ميل ≤85° — لا رؤية من الأسفل كعارض ثلاثيّ صرف؛ كافٍ للاستعراض فوق الأرض.)
- **سياق:** نماذج Sketchfab المطلوبة محجوبة بتسجيل الدخول (لا أُسجّل دخولاً/أُدخل اعتمادات) — يلزم تنزيلها بحساب المستخدم ثم رفعها، أو روابط CC0 مباشرة لأجلبها. مكتبة النماذج حسب النوع + التحكّم بالأبعاد تُبنى عند توفّر النماذج.

### م9.7.5 — خطّ نماذج glb واقعية حسب النوع (بنية تحتيّة) + إصلاح خدمة الملفّات (2026-06-24)
- **بحث موسّع** عن نماذج glb CC0 ذات تنزيل مباشر بلا تسجيل → **النتيجة الصريحة:** المتاح مباشرةً = نماذج **low-poly مكسوّة (Kenney City Kit CC0)** — أرقى من الإجرائيّ لكنها **ليست فوتوريالستك**؛ الفوتوريالستك محجوب خلف تسجيل Sketchfab. (نماذج Sketchfab التي اقترحها المستخدم تعذّر سحبها — تسجيل دخول إلزاميّ، لا أُدخل اعتمادات.)
- **البنية التحتيّة المبنيّة (تبقى للرفع المستقبليّ):** `TYPE_MODELS` (مكتبة glb حسب النوع في `public/models`) + فرع glb في `buildTowerLayers` (ScenegraphLayer + PBR + roll 90) + `TowerItem.glb` + `scripts/inspect-glb.ts` (أبعاد النموذج لحساب الملاءمة) + ثلاثة نماذج CC0 (`tower/mall/hotel.glb`).
- **إصلاح `middleware.ts`:** استثناء امتدادات `glb|gltf|stl` من مُطابِق الـmiddleware (كانت تُعاد توجيهها لـ`/login`) — تُخدَم الآن 200 (مؤكَّد بـcurl/fetch).
- **عقبة قائمة:** عرض النموذج الثابت في deck يتلقّى HTML («<!DOC») رغم أنّ الملف يُخدَم سليماً (curl/fetch=glb · لا service worker) — لم تُحسَم في deck/loaders.gl هذه الجلسة.
- **القرار:** الإبقاء على **النماذج الإجرائية موثوقةً افتراضاً** (تُعرَض جيداً + مرافق + استعراض حرّ + حلقات تحت القاعدة)، وخطّ glb جاهز **لمسار الرفع** (روابط Supabase موقّعة مطلقة — يعمل) لنماذج Sketchfab الفوتوريالستك حين يرفعها المستخدم.
- **التالي:** حلّ عقبة عرض glb الثابت (أو الاعتماد على الرفع) · مكتبة النوع الفوتوريالستك عند توفّر نماذج المستخدم · علم الهيئة (مؤجَّل) · تحكّم بالأبعاد.

### م9.7.6–م9.7.8 — واقعيّة أعمق للنماذج + فندق متمايز + تحكّم المستخدم بالتوجيه والأبعاد والكاميرا (2026-06-24)
- **م9.7.6 (تظليل واقعيّ):** لون رأسيّ لكل النماذج في `freeze()` — **تدرّج قاعدة→قمّة + AO** بحسب الاتجاه (`colors` لكل رأس تضرب `getColor`) + **خامات Phong** (`MAT_BODY`/`MAT_GLASS`: `shininess 160`, `specularColor [190,232,255]`) بدل المنبعث الثابت + ألوان زجاج أغمق (لأنّه صار مُضاءً) — عمق ولمعان بلا كساء صور.
- **م9.7.7 (نبض حيّ + توهّج):** الحلقات النابضة فُصِلت لطبقة متحرّكة بـRAF تحت المجسّم (`buildRingLayers` · موجتان `ss 0.55→2.25` + تلاشي ألفا · خنق ~11 إطار/ث · بوّابة `z>13` لصون الأداء الحيّ) + طبقة توهّج زجاج خافتة فوق الزجاج المُضاء.
- **م9.7.8أ (فندق منتجع متمايز — `c504ca2`):** `generateHotel` أُعيد تصميمه: **برج شريحيّ عريض** (`tw=w·0.92, td=d·0.4`) على **قاعدة ٣ طوابق** (podium) + مظلّة مدخل (porte-cochère) + **مرافق** (extras): مسبح أزرق + سطح خشبيّ + حديقة + نخيل (`palm`) — متمايز بوضوح عن البرج السكنيّ.
- **م9.7.8ب (تحكّم المستخدم — هذه الجلسة):** هجرة `parcel_parametric_models` (إضافيّة، nullable): `rotation_deg/width_m/depth_m/height_m` (مطبَّقة بإذن صريح). **التوجيه 360°:** `cfg.rotationDeg → towerItems.yaw → meshLayer getOrientation:[0,yaw,0]`. **الأبعاد اليدويّة:** `width/depth/height` (فارغ = اشتقاق تلقائيّ من البصمة) تُمرَّر لـ`generateModel` وتدخل مفتاح التخبئة. **الكاميرا:** طيران القطعة المفترضة يقترب مستوى زوم إضافيّاً (`+1.1`، حتى `MAX_ZOOM=18`) ليملأ المجسّم الإطار كاستعراض درون. **الواجهة:** شريط تمرير دوران + حقول طول/عرض/ارتفاع في `ParametricSection` (للمدير).
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق حيّاً** (admin · «فندق 5 نجوم»): الواجهة تعرض الشريط والحقول؛ الحفظ ⇐ `POST upsert 200` ⇐ إعادة جلب بالأعمدة الجديدة 200 ⇐ لا أخطاء كونسول؛ الخريطة تعيد بناء المجسّم على تغيّر الإعداد (تبعيّة `parametricCfg`). أُعيد إعداد الديمو لقيمه الأساسيّة بعد التحقّق.
- **سياق قائم:** النماذج الإجرائيّة سقفها دون الفوتوريالستك (يلزم رفع glb من المستخدم — تسجيل Sketchfab محجوب) · علم الهيئة مؤجَّل بطلب المستخدم.

### قاعدة المعرفة — توثيق شامل عبر 26 وكيل أوبس + تجهيز وكلاء البناء القادم (2026-06-24)
- **الغرض:** طلب «وثّق كل المبنيّ بدقّة + جهّز 8 وكلاء قرّاء كمنفّذين للبناء القادم». الواقع التقنيّ: الوكلاء سريعو الزوال (لا يبقون «أحياء» بين الأدوار) — فالتجهيز الدائم = **قاعدة معرفة على القرص** يقرؤها كل وكيل بناء مستقبليّ أولاً ثم يبني.
- **الآليّة:** سير عمل من **26 وكيل أوبس 4.8** — 8 قرّاء عميقون (مجال لكلٍّ) → 8 مدقّقون مستقلّون يتحقّقون من كل ادّعاء مقابل الملفات الفعليّة (إنفاذ «صفر تأليف») → تصحيح عند الحاجة → تركيب فهرس + دليل تنفيذ → نقد اكتمال. (~3.2M توكن · 828 استدعاء أداة · ~31 دقيقة.)
- **المخرجات (`docs/نظام_المعرفة/`):** الفهرس ودليل التنفيذ (00) + 8 موجزات مدقّقة (دقّة التدقيق 88–97/100): الدستور/التصميم · البيانات/القاعدة/الأمان · الخريطة · القطع/المجسّمات · المستشار/الضوابط/الذكاء · الأعمال/طبقة العرض · المصادقة/القشرة/الإعدادات · الحالة/خريطة الطريق.
- **تعارضات مرصودة (المبدأ الحاكم: المبنيّ يفوز — يُصحَّح الوصف):** لون «مفترضة» المبنيّ **#22C3F3** بينما 4 مواضع توثيقية ما زالت **#8B6FB0** (CLAUDE.md:41 · الوصف 337 و431 · هذا السجلّ سطر 78) · قائمة `م9_الخارطة_الاستثمارية.md:156` تؤشّر مبنيّاً كغير مبنيّ (المنسدلة/المكتبة) · الوصف:5 مزامَن «حتى م8.5» والمبنيّ بلغ م9.7.8ب · تعليق `investment-map.tsx`~1024 يقول z>12.5 والفعليّ z≥13 · `m9-investment-map` غير مدموج في main (نصّ متأخّر).
- **فجوات (ناقد الاكتمال 82/100):** لا اختبارات E2E/تكامل (54 اختبار وحدة فقط على دوالّ نقية) · بنية RLS المتعاقبة عبر الهجرات تحتاج توثيقاً أعمق · فجوات منطق §ج.9 معروفة بلا اختبار (EXCLUDED_SECTORS فارغة → «خطر صمت»، مدخلا الشراكة العراقية/الإفراز السكنيّ غير ملتقَطين، `match_legal` دلاليّ بلا فلترة الوسوم الأربعة).
- **للبناء القادم:** كل وكيل ينفّذ مرحلة **يقرأ موجز مجالها من `docs/نظام_المعرفة/` أولاً ثم يبني** — هذه القاعدة هي «تجهيز الوكلاء» الدائم.

### م9.7.9 — كاميرا درون تؤطّر المجسّم كاملاً + حلقات تتدفّق من تحت حدّه + تصحيحات توثيق (2026-06-24)
- **تصحيحات توثيق (المبدأ الحاكم: المبنيّ يفوز):** لون «مفترضة» **#8B6FB0 → #22C3F3** في CLAUDE.md:41 والوصف (337 و431) وهذا السجلّ (78) · مزامنة الوصف «حتى م8.5 → م9.7.8ب» وتاريخه · قائمة `م9_الخارطة:156` حُدِّثت (المنسدلة/المكتبة/الحلقات النابضة/المرافق/الاستعراض الحرّ/التظليل/التحكّم بالأبعاد = مبنيّة) · تعليق `investment-map`~1024 (z>12.5 → z≥13، ~11 إطار/ث).
- **الكاميرا (الطيران): تؤطّر المجسّم كاملاً والزوم يتناسب مع حجمه/ارتفاعه** — `modelDimsRef` يحفظ بصمة وارتفاع كلّ مجسّم؛ `zoomForModel()` يحسب زوماً من الامتداد الظاهر الأكبر (البصمة الأفقية أو إسقاط الارتفاع على الشاشة عند الميل 60°) ليملأ ~50% من أصغر بُعد للشاشة (هامش مريح، حدّ 13.5..18). فالمجسّمات العالية/العريضة تُبعِد الكاميرا والصغيرة تقرّبها — بدل الاقتراب المفرط السابق (parcel-fit +1.1).
- **الدرون: دورة كاملة 360° حول المجسّم** — على شوطين 180° خطّيّين متّصلين (إذ يُطبِّع MapLibre دوران 360° في easeTo واحد إلى صفر) — استعراض دوّار كامل بدل نصف دائرة، مع بقاء المركز/الزوم/الميل ثابتاً.
- **الحلقات النابضة تتدفّق من تحت حدّ المجسّم للخارج** (`buildRingLayers`): المقياس يبدأ عند **1×** بصمة المجسّم (لا 0.55× تحت جسمه) ويتمدّد إلى 2.7×؛ والشفافيّة `sin(π·p)` = **صفر عند الحافة** (مخفيّة تحت المجسّم) ثم تظهر بعد خروجها وتتلاشى بعيداً — 3 موجات متعاقبة. يحلّ تداخلها السابق مع المجسّم وتشويه رؤيته.
- **القبول:** typecheck + lint نظيف · **متحقَّق حيّاً جزئياً** (admin · فندق 5 نجوم): تشخيص الزوم صحيح (بصمة 111م/ارتفاع 67م ← زوم 17.7؛ ارتفاع جوّيّ 304م = تأطير قريب لا إقليميّ) ولقطة أظهرت المجسّم مؤطَّراً والحلقات تحت قاعدته · لا أخطاء كونسول.

### م9.7.10 — مباني مضيئة + حلقات تُحيط البصمة (لا تتقاطع) + تثبيت التحقّق البصريّ (2026-06-24)
- **سياق:** ملاحظة المستخدم أنّ التحسينات لم تظهر. السبب الجذريّ للالتباس: نافذة معاينة deck.gl فسد حالتها بعد إعادات تحميل HMR متكرّرة (تحذير `luma.gl already initialized`) فظهرت أرضية فارغة بلا مجسّم — **خادم معاينة جديد حلّ المشكلة وأظهر المجسّمات بوضوح**.
- **مباني مضيئة (model-render.ts):** تفتيح لوحات الألوان (الهيكل/الزجاج لكلّ نوع) + تقوية توهّج الزجاج المنبعث (ألفا 64→125، +75/90/85) + رفع الإضاءة (`MODEL_LIGHTING`: ambient 1.25→1.6 · sun 1.7→2.1 · fill 0.65→0.9) — المبنى الآن مضيء وبارز لا كتلة داكنة.
- **حلقات تُحيط البصمة كاملةً (investment-map.tsx):** نصف قطر الحلقة الأساس = `0.5·hypot(fw,fd)·1.2` (قطر البصمة + هامش) بدل `max(fw,fd)·0.5` — فلا تتقاطع مع زوايا المبنى؛ مع بدء المقياس 1× والشفافية `sin(π·p)` تبقى الحلقات **خارج حدّ المجسّم تماماً، تتدفّق من تحته للخارج**.
- **تثبيت الكاميرا:** عند عدم تحمّل أبعاد المجسّم بعد (سباق تحميل) → زوم احتياطيّ آمن ≤16.5 بدل التقريب المفرط لـ18.
- **القبول ✓ متحقَّق بصرياً (خادم معاينة جديد، admin):** لقطتان — **البرج** «مبنى تجاري تسوقي» (متعدّد الطوابق، مضيء، نوافذ/زجاج، مؤطَّر كاملاً، حلقة سماوية تتدفّق حول قاعدته) و**الفندق** «فندق 5 نجوم» (منتجع عريض مضيء + نخيل/حدائق عند القاعدة + حلقات تحيط القاعدة) — البنود الثلاثة تعمل بصرياً على المجسّمين. typecheck + lint نظيف.
- **ملاحظة:** ظهور «أرضية فارغة» في أولى محاولات الطيران بعد إقلاع الخادم = سباق تحميل بيانات (الطيران قبل جهوز طبقات deck) — يزول فور تحميل البيانات (لقطات البرج/الفندق على خادم مستقرّ تؤكّده).

### م9.7.11 — استعراض درون سينمائيّ مثبّت (ارتفاع جوّي ثابت + كوريغرافيا ميل) + حلقات نابضة أنيقة (2026-06-24)
- **الكاميرا (تأطير مقود بالارتفاع — مثبّت):** `zoomForModel()` أُعيدت لتعكس **صيغة مؤشّر الارتفاع نفسها** (`alt = 1.5·ارتفاع‌الكانفس·156543·cosφ / 2^z`) فتقف الكاميرا على ارتفاع جوّيّ مستهدَف = `max(1200, الامتداد·6)` متر — حدّ أدنى **1.2كم** (لا اقتراب مفرط) ويتناسب **طردياً** مع حجم المبنى (الكبير يرتفع أكثر فيبقى ~25% من الشاشة مع محيطه). الأبعاد من المجسّم إن توفّرت وإلّا **من بصمة القطعة** (حتميّ لا يعتمد على تحميل الشبكة). `MAX_ZOOM=18`.
- **الدرون: نصف دورة 180° باتجاه واحد، بلا توقّف، بكوريغرافيا ميل** — حلقة RAF واحدة عبر `jumpTo` (لا `easeTo`/`flyTo` داخليّ، فلا أخطاء `_onEaseFrame`/`already running`). المنحنى `easeOutSine` (انطلاق فوريّ سلس متّصل بالاقتراب ثمّ تباطؤ مهيب للاستقرار). الميل بدلالة **الدرجات المقطوعة**: 0–45° تصوير جوّيّ (60°) → 45–90° نزول للأفق (82°) → 90–135° أفقيّ → 135–180° **صعود يعود للتصوير الجوّيّ (60°) ويستقرّ**. الاقتراب ينتهي جوّياً (60°) فيبدأ الدوران من الزاوية الجوّية. (`HORIZON_PITCH=82` · `SWEEP_MS=7500`.)
- **معالجة فيضان الكونسول:** حلقة توهّج حدود نينوى تتخطّى التحديث أثناء حركة الكاميرا (`isMoving()`) فلا تُعيد الدخول لحلقة رسم MapLibre أثناء الطيران؛ + إلغاء `droneRafRef` عند طيران جديد/مغادرة التركيز/التفكيك (تنظيف RAF).
- **حلقات نابضة أنيقة (`model-render.ts` + `parametric-tower.ts`):** لون أهدأ أقلّ حدّة `[60,180,240]→[100,165,205]` · شفافية أعلى وغلاف أنعم `a = 180·sin(π·p)^1.35` (بدل `235·^0.85`) · سُمك أرقّ متّسق `halfW = max(0.4, gap·0.06)` (بدل `·0.16`) — نسبة سُمك/نصف‑قطر ثابتة عبر الموجات = تناسق بصريّ. **بنفس تساريع التمدّد** (`ss = 1+1.7·p`، 3 موجات) فلا تتغيّر سرعة التدفّق.
- **القبول:** typecheck + lint نظيف. محلّيّ فقط (بانتظار «ادفع»).

### م9.7 (إضافات) — القمر الصناعي افتراضياً + حلقات أغمق وأوسع ✓ متحقَّق بصرياً (2026-06-24)
- **القمر الصناعي هو الوضع الافتراضي** (طلب المستخدم): `DEFAULT_BASE` + `DEFAULT_SETTINGS.default_base` → `"satellite"` (الكود)، و**تحديث صفّ الإعدادات الفعلي** `default_base='satellite'` (`scripts/set-default-base.ts` — لأن إعداد المستخدم المحفوظ يتقدّم على ثابت الكود، `investment-map` يطبّق `settings.default_base` عند الإقلاع).
- **حلقة المفترضة أغمق:** `PING_HEX.assumed` → **`#0E5FB0`** (أزرق غامق من نفس العائلة) لتبرز بوضوح على صور القمر الصناعي.
- **مدى/قطر أوسع عند التقرّب الشديد:** معامل `pingScale` يستمرّ بالنموّ فوق `PING_Z_IN` (z12.5) حتى **~3×** عند آخر درجات الزوم (`+min(2.0,(z-12.5)·0.55)`) بدل التوقّف عند 1×.
- **حلقات أوضح وأبرز (`globals.css`):** سماكة الحلقة `2.5→3.25px` · **هالة مزدوجة أقوى** (`0 0 10px, 0 0 26px 3px currentColor`) · ذروة العتمة `0.8→0.95` — لتبرز على صور القمر الصناعي. (العلاقة «الحلقات تحت المجسّم والمجسّم أغمق منها» تكتمل ببناء البرج البارامتري — م9.7.1ج.)
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق بصرياً** (قمر صناعي · 578م): الأرضية الزرقاء الرقمية + **الحلقات الزرقاء الأغمق الأوسع** بارزة على صور القمر الصناعي (لقطة محفوظة).

### م9.8 — مزوّد قمر صناعي أوضح/أحدث (raster مُمرَّر ضمن MapLibre) (2026-06-24)
- **السبب:** صور MapTiler «hybrid» وEsri غير محدَّثة فوق الموصل (طلب المستخدم: «أحدث خارطة قمر صناعي»).
- **بحث موسّع (7 وكلاء · بحث ويب):** (1) **Esri «سيئة» = مجمَّدة لا منخفضة الدقّة** — التقاط الموصل **2021-07-20 (WV‑02)** مؤكَّد من خادم Esri نفسه، وحتى أحدث إصدار Wayback يعيد نفس بكسلات 2021 (أقدمها 2017) ⇒ لا بكسلة أحدث لدى Esri. (2) **Mapbox غير متوافقة معمارياً** — شروطها §2.8.1 تمنع التمرير عبر وسيط و§3.56 تشترط محرّك Mapbox GL JS (وMapLibre ليس منه) ⇒ **أُزيلت**. (3) **Google Map Tiles API الرسمية مسموحة في محرّك طرف ثالث** (صفحة سياساتها تشرح وضع شعار Google «عند استخدام محرّك طرف ثالث») — مختلفة عن خرائط Google الاستهلاكية/البلاط المسروق `/vt` الممنوع — وهي **الأوضح/الأحدث** (Maxar ~0.5م · تحديث 1-3 سنوات). البديل الأرقى = Airbus OneAtlas (0.3م، مدفوع غالٍ)؛ لا بديل مجاني أوضح (Sentinel‑2 = 10م ضبابي).
- **بحث الأسعار (6 وكلاء):** أفضل ٤ مدفوعين يعملون مع MapLibre عبر وسيط: **Airbus** (30سم · تكليف حديث · €500 تجربة 30يوم · ~200-500$/شهر) · **Google** (~مجاني · 100k/شهر + 300$/90يوم) · **Azure** (~0-15$/شهر · 200$/30يوم · مصدر Airbus) · **Esri** (مجاني دائم لكن مجمّد 2021). مُستبعَدون: Maxar/Planet (حظر صور العراق 2026 + ترخيص) · Mapbox (شروط).
- **أربعة مسارات وسيط** (كلها خلف `hasSession`، المفتاح خادمي حصراً — القاعدة 6): عامّ `[provider]` (Esri، تمرير بسيط) + مخصّص `google` (`createSession` مخزَّن + z/x/y + **`no-store`**) + `azure` (subscription-key · z/x/y عبر معاملات الاستعلام · `microsoft.imagery`) + `airbus` (OAuth: api key→bearer مخزَّن + قالب WMTS من التجربة). منطق العناوين معزول في `imagery-proxy.ts` + اختبار وحدة. متغيّرات: `ESRI_API_KEY` · `GOOGLE_MAPS_KEY` · `AZURE_MAPS_KEY` · `AIRBUS_API_KEY`+`AIRBUS_WMTS_TEMPLATE`.
- **`map-config.ts`:** `SatelliteProvider = "maptiler"|"esri"|"google"|"azure"|"airbus"` + `SATELLITE_PROVIDER = "maptiler"` (**افتراضي آمن للإنتاج**) + `IMAGERY_SOURCES` لكلٍّ (قوالب · tileSize · maxzoom · نسب).
- **`buildStyle(base,data,provider)`:** للقمر بمزوّد غير-MapTiler ⇒ `buildSatelliteStyle`: طبقة raster أسفل + خطوط/سبرايت MapTiler + **طبقاتنا السيادية فوقها** (§هـ.4 محفوظ). `switchBase(next,force)` يعيد البناء عند تبديل المزوّد. شريط مقارنة (MapTiler/Esri/Google/Azure/Airbus) **تطوير فقط** وحين القمر فعّال.
- **القبول:** typecheck + lint نظيف · 12 اختبار وحدة أخضر · المسارات الأربعة مُركَّبة (401 بلا جلسة، بلا تعارض توجيه static/dynamic) · **القاعدة الافتراضية بلا تغيير (maptiler) = صفر ارتداد إنتاجيّ**. محلّيّ فقط (بانتظار «ادفع»).
- **مُعلَّق (مفاتيح المستخدم):** المستخدم يضع `GOOGLE_MAPS_KEY` · `AZURE_MAPS_KEY` (حساب Azure Maps Gen2) · `AIRBUS_API_KEY`+`AIRBUS_WMTS_TEMPLATE` (من تجربة OneAtlas، يُختار الموصل) في `.env.local` بنفسه — **لا أُنشئ حساباً ولا أُدخل مفتاحاً**. ثم اختبار محلّي عبر الشريط، واختيار الفائز ⇒ تبديل `SATELLITE_PROVIDER` + **شعار/إسناد المزوّد في الواجهة (إلزام ToS — لم يُضَف بعد)** + تحديث سطر الحزمة في CLAUDE.md.
- **تشخيص Google قاطع (2026-06-25 · مؤجَّل):** المفتاح صحيح في `.env.local`، Map Tiles API مفعّل، قيود المفتاح سليمة (App=None · يشمل Map Tiles)، والكود/الوسيط مثبَت صحيحاً. **العائق الوحيد = الفوترة:** بطاقة الدفع لا تعمل على مشروع `My First Project` — أُثبِت حيّاً بنداء Geocoding بنفس المفتاح: `REQUEST_DENIED — "You must enable Billing"` (وcreateSession ⇒ 404). **مؤجَّل بطلب المستخدم حتى توفّر بطاقة عاملة.** عند تفعيل الفوترة: يعمل Google فوراً بلا أي تغيير كود (إعادة تحميل + زرّ Google، أو `SATELLITE_PROVIDER="google"`)؛ يتبقّى حينها: شعار/إسناد Google + حذف `console.error` التشخيصي «[google createSession]» من مسار google + تحديث CLAUDE.md. (وسيط google مُقوّى: إعادة محاولة + تمسّك بأوّل جلسة.) Azure/Airbus محجوبان على تسجيل دخول المتصفّح.

### م9.8 (مكتبة البناء · المرحلة 1) — مكتبة مجسّمات إجرائية طبقيّة + وصف ممنهج (2026-06-25)
- **السبب:** المستخدم اعتمد تطوير المولّد المتجانس (م9.7) إلى **مكتبة بناء متقدّمة بمعماريّة برمجية قوية + ملف وصف ممنهج**، لرفع واقعية المجسّمات وتفاصيلها ومرافقها (مسابح/مواقف/حدائق/شوارع/محلّات) — خصوصاً المول والفندق. الأساس **إجرائيّ (كود)**: حتميّ · يتلاءم مع البصمة · بلا عقبة عرض glb.
- **المرحلة 1 (إعادة هيكلة حرفيّة · صفر ارتداد بصريّ):** أُنشئت `src/features/map/lib/building/` بطبقات: `types` · `geom` (بدائيّات + نبات + ثوابت) · `materials` (سجلّ خامات) · `facade` (`buildFace`/`buildMallFace`) · `recipes` (tower/mall/hotel + `generateModel`) · `ground` (حلقات + ظلّ) · `index`. الكود **منقول حرفياً** فمخرجات `generateModel` (الشبكات الستّ + extras) مطابقة. `parametric-tower.ts` صار **shim إعادة تصدير** (صفر كسر لمواقع الاستيراد الأربعة).
- **عقد العرض مُجمَّد:** `model-render.ts` و`investment-map.tsx` **بلا تغيير** — الخريطة تُعرَض كما هي تماماً.
- **وصف ممنهج:** `docs/مكتبة_البناء.md` (المبدأ · المعماريّة الطبقيّة · سجلّ الخامات · البدائيّات · الواجهات · المرافق · تشريح كل نوع · قواعد الأبعاد · الحتميّة · معايير الجودة · خارطة المراحل) = مصدر الحقيقة للمكتبة.
- **القبول:** typecheck + lint نظيف · `building/building.test.ts` (بنية + حتميّة، لا `Math.random`) أخضر · **٦٨ اختباراً كلّها خضراء**. محلّيّ فقط.
- **القادم:** المرحلة 2 (المول الفاخر: بدائيّات `dome`/`arch`/`louver`/`railing` + facilities `parking`/`garden`/`street`/`plaza` + إثراء الوصفة) = أوّل مكسب بصريّ؛ ثمّ المرحلة 3 (الفندق الفاخر).

### م9.8 (الحلقات) — حجب نبضة مركز المجسّم عند ≤2كم جوّي ✓ معتمد (2026-06-25)
- **الطلب:** عند ارتفاع جوّي **≤2كم** تختفي النبضة في **قلب المجسّم/مركز القطعة** (نبضة الدبوس CSS) للقطع المفترضة، ويبقى فقط ما ينبض من **حدّ البصمة الخارجي** (حلقات deck `buildRingLayers`)؛ فوق 2كم بلا تغيير.
- **التنفيذ (`investment-map.tsx` · دالّة `compute` للنبضات):** يُحسَب الارتفاع الجوّي حتمياً (نفس صيغة مؤشّر «الارتفاع الجوي»: `(0.5/tan(FOV/2))·ارتفاع‌الكانفس·156543·cosφ/2^z`)؛ فإن ≤2000م تُحجَب نبضة الدبوس للقطع `kind==="assumed"` فقط — **التسمية وحلقات deck بلا مساس**. يتحدّث لحظياً على حدث الزوم.
- **القبول ✓:** typecheck + lint نظيف · **متحقَّق من المستخدم بصرياً** («ممتاز»). محلّيّ فقط.

### م9.8 (إعادة التصميم الكبرى للمول + الأساس البصريّ) — توثيق شامل (2026-06-26)
> أُنجِزت بالتعاون التفاعليّ مع المستخدم (مراجعة بصريّة متكرّرة عبر لقطات). محلّيّ فقط حتى «ادفع». (سبقها: تنفيذ نموذج «سكنيّ» منفصل ثمّ **إلغاؤه جراحيّاً** بطلب المستخدم — رجوع تامّ للأنواع الثلاثة؛ والمول/الفندق أُبقيا على وضعهما حينها.)

**أ) الأساس البصريّ (عامّ — يمسّ كلّ المجسّمات):**
- **ظلال مُلقاة:** شمس `DirectionalLight({ _shadow: true })` + خفض المحيط (`investment-map.tsx · MODEL_LIGHTING`) + `shadowColor` ناعم → عمق وترسيخ. **التوهّج Bloom مؤجَّل** (لا وحدة جاهزة في luma.gl v9؛ يلزم شيدر مخصّص — لم يُنفَّذ تفادياً لمخاطرة المحرّك الحيّ).
- **خامات (Textures):** خطّ أنابيب كامل — إحداثيّات UV (إسقاط ثلاثيّ المحاور) في `freeze` + **مولّد خامات إجرائيّ بـCanvas** (`building/textures.ts`: facade/glass/tile/metal/asphalt، **أبيض الأساس** فيحفظ لون القناة) + ربطها في `model-render.ts` (الهيكل=facade · الزجاج=glass · extras عبر حقل `tex`). عميل فقط، حتميّة، مخبّأة كـdataURL.
- **توهّج الزجاج:** تمريرة توهّج إضافيّة على `glassB` (كما على `glassA`) → الزجاج يبرق ولا يُغسَل رماديّاً. + **خفض شدّة الإضاءة** كي تقرأ الألوان مشبعة.

**ب) المول (`generateMall`) — إعادة تصميم ثوريّة:**
- **الكتلة:** **٤ مستويات** (أرضيّ مزدوج الارتفاع + ٣ طوابق) · **قلب أرضيّ غائر** (`bandDepth`) يفتح نطاق محلّات زجاجيّاً حول المحيط (رؤية حقيقيّة للداخل بلا CSG) + كتلة علويّة كاملة كسقف.
- **الواجهة (`buildMallFace` v2):** أرضيّ **زجاج رؤية شفّاف** بتجهيز داخليّ مضيء (جدار إضاءة دافئ + سقف مضيء + كاونتر + رفوف + أشخاص) · ط1-3 **ألواح زجاج بارزة** بكاشفات جانبيّة/علويّة/سفليّة ومفاصل (نتوء + AO).
- **المداخل:** **جناحا مدخل مزجّجان بكامل ارتفاع المبنى** على الجهتين المتقابلتين (الكراج +Y والمقاهي -Y) بمونتينات وأحزمة طوابق متوهّجة وتتويج ومظلّة.
- **بروزات معماريّة:** أعمدة زاويّة **كحليّة** كاملة الارتفاع (ترتفع لقمم المرساتين) + زعانف عموديّة على الجهات الأربع + كرنيش تتويج كحليّ محيط.
- **التتويج:** **قاعدة دائريّة زجاجيّة واسعة** (طبلة كريستال زرقاء بنوافذ على كامل الارتفاع/المحيط بمونتينات كحليّة + **غطاء قرص أفتح** `crystalLight` يحيط القبّة) تتمركز فوقها **قبّة كريستاليّة زرقاء** (`crystal`) قطرها موسّع، بأضلاع معدنيّة (قطع زجاجيّة) + سارية + فانوس.
- **السطح:** المرساتان (الجانبان البارزان) مغلّفتان بزجاج · **بلاط سطح رماديّ** (`roofTile`) · **صفّ تكييف مركزيّ منسّق صغير** على الحافّة.
- **الموقع:** **بورسلين أبيض لامع** (`porcelainApron`) على الجهات الثلاث + فناء أماميّ يسدّ فراغ الكراج، ممتدّ لحدود الحدائق · **نافورتان** (`fountain`) + حدائق وحلقات أشجار · **ساحة خلف** بسارية وبانر إعلانيّ مضيء (`signMast`) + **مقاهٍ فاخرة** (`cafeTerrace`) + **حشد** (`crowd`) · **كراج أسفلت رصاصيّ مخطّط** بسيّارات واقعيّة بلونين وزجاج (`realCarsLot`) · **سور محيطيّ مهيب** (رخام سفليّ + سياج حديديّ مزخرف · `perimeterWall`) + **بوّابة حديديّة** مزخرفة + **كشك حرس**.

**ج) الثيمة اللونيّة (المول):** هيكل **أبيض** `[242,242,244]` · نوافذ **زرقاء براقة** `glassB [56,146,224]` (+توهّج) · لمسات **كحليّة** `navy` (أعمدة/زوايا/كرنيش) · قبّة **كريستال أزرق** + غطاء أفتح · أحزمة سماويّة.

**د) الكاميرا:** المول **حصراً** يُؤطَّر من **900م** (بدل ~1400م) — أُضيف `kind` إلى `zoomForModel` ويُمرَّر من `modelDimsRef` (يخزّن النوع)؛ بقيّة الأنواع وأسلوب التصوير **بلا تغيير**.

**هـ) خامات جديدة (`materials.ts`):** `porcelain · bannerLight · crowd · carBody · carAccent · carGlass · fabric · navy · crystal · crystalLight · roofTile · marble · iron` (+ خامات سكنيّة سابقة بقيت معرّفة).

**و) بدائيّات/مرافق جديدة:** `building/textures.ts` · geom: `disc` + تحسين `tree` (جذع مدبّق + تاج كثيف ٩ فصوص) · facilities: `porcelainApron`/`fountain`/`cafeTerrace`/`crowd`/`signMast`/`realCarsLot`/`perimeterWall`.

**ز) القبول:** typecheck + lint نظيف · `building.test.ts` **٣ اختبارات** (بنية + حتميّة + إحداثيّات منتهية بلا NaN وميزانيّة مضلّعات <220k) خضراء · المول ≈ 26k+ مثلّث / 24+ طبقة (أداء جيّد) · **مراجعة بصريّة متكرّرة معتمَدة من المستخدم**. محلّيّ فقط — لا مساس بالقاعدة ولا الإنتاج.

**ح) التالي:** تطبيق الثيمة (الزجاج/الألوان/الثيمة العامّة) على **البرج والفندق** كلٌّ بما يلائمه؛ ثمّ اختياريّاً التوهّج Bloom بشيدر مخصّص.

### م9.8 (إعادة تصميم البرج + سلوك المواقع متعدّدة المجسّمات) ✓ معتمد بصرياً (2026-06-26)
> أُنجِزت بمراجعة بصريّة تفاعليّة متكرّرة عبر لقطات المستخدم، وأُقِرّت بـ«التعديل ممتاز وثّقه واعتمده». محلّيّ فقط حتى «ادفع».

**أ) البرج (`generateTower` + `buildTowerFace` + `model-render`):**
- **طابع أزرق:** هيكل أزرق فاتح **مطفأ** (لمعان منخفض، بلا توهّج ضبابيّ) — `MAT_BODY_BY_KIND.tower` مطفأ.
- **زجاج الطوابق = مادّة المكعّب العلويّ:** كلّ نوافذ البرج تُرسَم بـ`TOWER_CRYSTAL = [96,172,255]` المنبعث (نفس المكعّب الزجاجيّ أعلى البرج).
- **الجزء السفليّ العريض:** نوافذ **بارزة (bay)** + شُرفات بحاجز زجاجيّ (`buildTowerFace`).
- **الجزء العلويّ الأنحف:** **ستارة زجاج كاملة** بمفاصل **أفقيّة فقط** تقسّمه إلى **٥ قطع فوق بعضها** لكلّ جانب (بلا تقطيع عموديّ).
- **التتويج:** أُلغيت القبّة؛ **المكعّب العلويّ الصغير زجاج أزرق كريستاليّ**.
- **القاعدة/البوديوم + شرفة النكسة:** **رماديّ سماويّ مطفأ** (`skyGray`) + سياج زجاجيّ على الشرفة.
- **الساحة:** بلاط **أزرق-رماديّ فاتح جدّاً** (`skyTile`) **بقطع بارزة** (مفاصل غائرة) + درج مدخل (بلا حدائق/بوّابة للمفرد).

**ب) الفندق:** هيكل كريميّ دافئ فاتح + زجاج أزرق منقّى + **تتويج ذهبيّ** (هويّة المنتجع) + **فانوس قبّة كريستاليّ**.

**ج) المواقع متعدّدة المجسّمات (count>1 · `investment-map.tsx`):**
- **حلقة نابضة مركزيّة واحدة:** الموقع كيانٌ واحد — المجسّمات بلا حلقات فرديّة، وحلقة واحدة مركزيّة تُحيط الكلّ بنصف قطر `0.5·hypot(wM,dM)·1.05` (نفس ستايل المفرد). المفرد بلا تغيير.
- **حديقة ممرّ بين الصفوف (أبراج · ٤ فأكثر):** كيان `generateGardenStrip` (عشب + ممرّ + صفّا أشجار + صفّا شجيرات، متناظر كامل) يُوضَع في كلّ ممرّ بين صفّين، على **منصّة مرتفعة** تحجب بلاط الساحات تحتها فيظهر كاملاً. البرج المفرد بلا حديقة.

**د) خامات جديدة:** `skyTile` (بلاط أزرق-رماديّ فاتح) · `skyGray` (رماديّ سماويّ مطفأ) · `crystalLight` (غطاء قاعدة قبّة المول الأفتح). + `MAT_GLASS_BY_KIND` + `TOWER_CRYSTAL` في `model-render`.

**هـ) القبول ✓:** typecheck + lint نظيف · `building.test.ts` ٣ اختبارات خضراء (بنية + حتميّة + ميزانيّة <220k) · حارس **سقف بلاطات** (≤720) للأداء · **معتمَد بصرياً من المستخدم**. محلّيّ فقط — لا مساس بالقاعدة ولا الإنتاج.

### م9.9 — سلاسة العرض + انعكاسات وزجاج كحليّ + إعادة تصميم الفندق (المنتجع) + متانة الخريطة ✓ معتمد بصرياً (2026-06-27)
> أُنجِزت بمراجعة بصريّة تفاعليّة متكرّرة عبر لقطات المستخدم، وأُقِرّت بـ«وثّق البناء واعتمده». محلّيّ فقط على فرع `m9-investment-map` — لا دفع حتى «ادفع».

**أ) سلاسة العرض «الزبدة» (صفر تغيير بصريّ — [investment-map.tsx](src/features/map/components/investment-map.tsx) + [model-render.ts](src/features/map/lib/model-render.ts)):**
- **تخبئة مراكز القطع** (centroid cache بمفتاح `ref_id`) — بدل `centroid()` لكلّ قطعة كلّ إطار في مؤثّرات النبضات/التسميات/الشارة.
- **instancing للنُسخ** (`count>1`): طبقة instanced واحدة لكلّ mesh بدل `count×N` طبقة منفصلة (مواقع/توجيهات لكلّ نسخة).
- **دمج المرافق المعتمة** (opaque extras merge) في طبقة العرض — خبز اللون في ألوان الرأس + `getColor=أبيض`؛ المرافق الشفّافة تبقى منفصلة بترتيبها. الدمج **خارج `generateModel`** (يصون اختبار الميزانيّة).
- **خنق الحركة** `MOTION_THROTTLE=32` (~30fps) لإعادة حساب النبضات/التسميات أثناء الطيران/مدار الدرون؛ المواضع تبقى ملتصقة كلّ إطار عبر `positionPingEl` فلا ارتجاف.
- **سقف DPR للجوّال** `useDevicePixels` (canvas الـ3D فقط؛ الأساس يبقى DPR كامل) — تحت-إدراكيّ.

**ب) متانة الخريطة (حصّة MapTiler · [investment-map.tsx](src/features/map/components/investment-map.tsx)):**
- **أساس احتياطيّ تلقائيّ:** عند 429/5xx على `style.json` تُعاد المحاولة ثمّ يُبنى `fallbackStyle` (خلفيّة كحليّة + raster Esri + خطوط الحدود، **بلا glyphs/sprite من MapTiler** التي كانت تحجب حدث `load`). تُكتَم رسائل الأخطاء بعد التنبيه مرّة (`mapDegraded`).
- **التحميل المسبق معطَّل** (`PREFETCH_ENABLED=false`): كود `prefetchOverview` (raster z8–z11 + عمق حول المواقع · توقّف عند 429) محفوظ ومجهَّز لإعادة التفعيل بعد ترقية الباقة؛ البديل بلا كلفة = تدفّق عند الطلب + `maxTileCacheSize` أكبر.

**ج) مزوّد القمر الصناعيّ عبر متغيّر بيئة ([map-config.ts](src/features/map/lib/map-config.ts) + [.env.example](.env.example)):** `NEXT_PUBLIC_SATELLITE_PROVIDER` (maptiler|esri|google|azure|airbus · افتراضي maptiler) — تبديل بلا تعديل كود. جاهزيّة Google (مفتاح + session token خادميّاً عبر `/api/imagery/google`؛ يلزم للإنتاج شعار Google + إسناد Maxar).

**د) انعكاسات سماء إجرائيّة تحليليّة ([reflect-extension.ts](src/features/map/lib/reflect-extension.ts) — جديد):** `EnvReflectExtension` (LayerExtension تحقن خطّافات deck في SimpleMeshLayer). **بلا خامة/جهاز/دورة حياة** ⇒ كلفة ضئيلة وحتميّة. تلتقط العاديّ ومتّجه النظر في `vs:DECKGL_FILTER_COLOR` (varyings خاصّة) ثمّ تحسب `reflect()`→سماء متدرّجة + حافّة فرينل في `fs` (مزج محدود ≤1 — لمعان دون غسل اللون لأبيض). نُسختان: `ENV_REFLECT_GLASS` (انعكاس قويّ برّاق) · `ENV_REFLECT_BODY` (أهدأ حافّيّ). تُمرَّر صراحةً للزجاج/الهيكل فقط عبر معامل `reflect?` في `meshLayer`.

**هـ) زجاج كحليّ لكلّ المباني ([model-render.ts](src/features/map/lib/model-render.ts)):** `GLASS_BLUE_A=[58,112,206]` / `GLASS_BLUE_B=[34,80,180]` (مائل للكحليّ لامع — كزجاج القبّة). أُزيل تخصيص `TOWER_CRYSTAL/MAT_GLASS_BY_KIND`؛ كلّ الزجاج المضاء يأخذ `ENV_REFLECT_GLASS` + توهّج.

**و) إعادة تصميم الفندق ٥ نجوم (المنتجع · `generateHotel` في [recipes.ts](src/features/map/lib/building/recipes.ts)):**
- **بوديوم بزجاج كحليّ بمفاصل/مونتينات** (`mull` كحليّ `[24,38,70]` عموديّ كلّ 3.2م + ٣ أحزمة أفقيّة على ٤ جهات) + كرانيش ذهبيّة.
- **برج ثلاثيّ التدرّج** (`buildFace` لكلّ طابق + شُرفات نكسة ذهبيّة).
- **مهبط هليكوبتر** على السطح (حلقة ذهبيّة + قرص داكن + حرف **H** + أضواء محيطيّة `padLight`) — **بدل القبّة**.
- **منصّة مرتفعة موسَّعة 1.5×** جهة المسبح (`platDepth=d*1.5`) — **المسبح فوقها** (مستطيل ضمن حدود القطعة، حافّة رخاميّة ٤ جهات، ماء + خطوط مسارات، بلا نصف-دائرة) + باب زجاجيّ يطلّ على المسبح + درج رخاميّ + ١٤ أريكة ومظلّات وطاولات كافيه.
- **كراج سيّارات أسفلت رماديّ داكن** (`realCarsLot` بسيّارات حقيقيّة · الأسفلت `[54,57,64]` **بلا كساء** — الكساء كان يستبدل اللون فيُبيّضه) + **ممرّ مدخل أسفلت داكن متّصل بالكراج** + بورت-كوشير + باب مدخل.
- **سور محيطيّ ٤ جهات** (رخام + حديد + أعمدة) مع **بوّابة جهة الكراج** (مصراعان بقضبان) + **نقطة أمن** (booth: قاعدة رخام + زجاج + سقف ذهبيّ).
- **شريطا حدائق يلامسان السور** (بلا فراغات) + **٤٠ شجرة كثيفة** (نخيل/أشجار حقيقيّة + شجيرات).

**ز) ارتفاع طيران المول → 1200م** (`zoomForModel`: `kind==="mall" ? 1200 : …`) — استعادة الإطار الجوّيّ الأوسع.

**ح) صقل الحلقات النابضة (سونار أنيق · [ground.ts](src/features/map/lib/building/ground.ts) + [globals.css](src/app/globals.css)):** حلقة أرضيّة بنصف قطر وحدويّ + نطاق توهّج جرسيّ بمزجٍ جمعيّ (لا أسود/مربّع على القمر الصناعيّ · `shadowEnabled:false`) ونصف القطر لكلّ موجة عبر `getScale` (instancing). نبضة الدبوس: توقيت خطّيّ + نهوض ناعم ثمّ خفض متواصل للشفافية حتى 0 (تلاشٍ متناسب بلا انطفاء مفاجئ).

**ط) القبول ✓:** typecheck + lint نظيف · `building.test.ts` ٣ اختبارات خضراء (بنية + حتميّة + ميزانيّة <220k) · **معتمَد بصرياً من المستخدم**. محلّيّ فقط — لا مساس بالقاعدة ولا الإنتاج.

**ي) التالي (مُتّفَق):** **مسار عرض أوتوماتيكيّ للمواقع بطيران سينمائيّ مميّز** (جولة تتنقّل تلقائياً بين القطع/المواقع: `flyTo` + مدار درون لكلّ موقع، مع تتابع سلس).

## خطة العمل القادمة (معتمدة من المستخدم — 2026-06-12)
1. **مراجعة معمقة للمستشار القانوني** (§هـ.5): تقوية الشقّين + الاسترجاع والاستشهاد، و**تغذيته بملفات يقترحها المستخدم وتُناقَش قبل الإدخال** (توسعة الطبقة القانونية وفق §ج.5 — وسم ومطابقة ثم تضمينات).
2. **مراجعة ترشيقية شاملة للتصميم والوظائف**: تقييم جودة البناء وترابط الوظائف، تحديد أي وظيفة/جزء بلا قيمة مهمة، واقتراح ترشيق يقلّل التداخلات — تقرير للمستخدم قبل أي حذف.
3. **تعريب تسميات قاعدة الخريطة**: ضمن الحزمة الثابتة (لا Google Maps — تغيير معماري كبير وشروط استخدامها تمنع عرض بلاطاتها خارج SDK الخاص بها): فحص أنماط/مصادر MapTiler الأغنى بـ`name:ar`، وتكثيف **طبقة التسميات المحرَّرة** (الحل السيادي §هـ.4 — ما يعرّفه المستخدم يظهر بالخريطة والبحث)، ومراجعة ترتيب coalesce للتسميات.

## المرحلة التالية: التجربة المحلية ← الاعتماد ← الدمج ← م6.5 النشر
1. تجربة المستخدم المحلية الشاملة على الخادم الإنتاجي (هذه الجلسة جهّزته).
2. عند الرضا: اعتماد م6+م7 ← دمج `m7-enhancements` في `main` بوسمَي `m6-approved`/`m7-approved`.
3. م6.5: النشر على Render (Build: `npm install && npm run build` · Start: `npm run start` · Node 22 · المتغيّرات الستّة) + فحص دخاني — **بقرار المستخدم حصراً**.
4. فجوات موثّقة بانتظار قرار: إرسال الاستشارة (بريد/رابط) · تغيير اسم المستخدم · مدخل التوصيات من السايدبار (§هـ.4) · خريطة PDF بديلة (Static API محجوبة بالخطة).
